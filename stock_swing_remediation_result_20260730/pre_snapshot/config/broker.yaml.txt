enabled: true
base_url: ${BROKER_BASE_URL}
timeout_seconds: 10
retry_attempts: 2
raw_partition: broker
# FIX-SOURCE-4: broker is required for reconciliation
required: true
failure_exit_code: 1
description: "Alpaca broker API — required for order reconciliation"
