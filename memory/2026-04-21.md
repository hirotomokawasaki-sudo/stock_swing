- stock_swing news coverage debugging found raw collection succeeds for all 8 requested symbols, but only 4 symbols are displayed in the console; root issue shifted from ingestion failure to display/pipeline selection mismatch.
- Added news coverage diagnostics foundations: collect_data now writes data/audits/news_collection_status.json with per-symbol reason/fallback, dashboard_service exposes raw_symbols_collected, displayed_symbols_collected, missing_symbol_reasons, and failure_reason_counts, and UI shows coverage reasons.
- Confirmed current missing symbols MRVL, CIEN, DELL, and RBRK are classified as collected_but_not_displayed, not api_error/no_company_news.
- Implemented initial balancing attempts in dashboard_service: _select_balanced_news_items and per-symbol capping in _load_external_news_items, but they did not resolve the 8->4 symbol drop, implying the bottleneck is later or diagnostics are not yet fully wired through API.
- Added a diagnostics payload in get_news() intended to report loaded_by_symbol, linked_by_symbol, and selected_by_symbol, plus a News Diagnostics UI section; API verification is still incomplete because the response returned empty diagnostics and needs direct DashboardService inspection next.
