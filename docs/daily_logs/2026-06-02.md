# 2026-06-02

## CF-2: フロントエンド stable fetch wrapper

### 実施内容
- `console/ui/api-client.js` に `fetchJsonStable` を実装（`_stableFetch` グローバルとして公開）
  - AbortController 8秒タイムアウト
  - 指数バックオフリトライ（最大3回: 500ms → 1000ms → 2000ms）
  - `inflight` Map で同一 URL の重複リクエスト排除
  - `lastGood` Map で最後の成功レスポンスをキャッシュ
  - 失敗時は `{ data: lastGood, stale: true, error }` を返す（UI を白くしない）
- `console/ui/app.js` の `loadData()` を raw `fetch()` → `_stableFetch()` に置き換え
  - `this.isStale` フラグで stale 状態を追跡
- `console/ui/index.html` に `#stale-badge` 追加（デフォルト非表示）
- `console/ui/style.css` に `#stale-badge` スタイル追加（黄色・点滅アニメーション）
- `updateStatusBar()` で stale 時にバッジを表示、復帰時に非表示

### 確認結果
- `api-client.js` / `app.js` ともに syntax OK
- 全フラグ確認済み: `fetchJsonStable`, `inflight Map`, `lastGood Map`, `AbortController`, `exponential backoff`, `_stableFetch`, `isStale`, `stale-badge`, `visibilityState`

---

## CF-3: ポーリング間隔最適化

### 実施内容
- `app.js` の `startAutoRefresh()` に `document.visibilitychange` リスナーを追加
  - タブ非表示中はポーリングをスキップ
  - タブ再表示時: 待機中なら即座にリフレッシュ実行
- ポーリング間隔は既存の市場時間ベース動的設定をそのまま維持
  - 週末: 1時間 / 市場時間外: 1時間 / 市場時間中: 5分 / その他: 30分

---

## Benchmark SPY 更新 cron 定期化

### 実施内容
- `stock_swing_update_benchmark_spy` cron 登録
  - スケジュール: 毎週火〜土 08:30 JST（daily_report 実行前）
  - スクリプト: `scripts/update_benchmark_data.py`（yfinance 経由、直近60日分）
  - job ID: `be4838ae-9d76-458a-aa1e-d3c968750d65`
  - 初回実行: 2026-06-03 08:30 JST
- 手動実行で動作確認: 2026-04-06〜2026-06-01 の40営業日取得成功

### 期待効果
- α/β/Sharpe の計算に使う SPY データが毎日自動更新される
- 6営業日分以上のデータが揃えば、分析タブで β が自動的に表示される

---

## check_cron_health.py regression 修正（朝一番）

- 昨日の修正が誤ったパス（`AI_Invest/scripts/`）に適用されていたため再発
- `scripts/check_cron_health.py` に正しく適用:
  - `reconciliation` の job_id を `32c3be71...` (market_hours) に更新
  - stale 判定を `lastRunAtMs` ベース → `nextRunAtMs` ベース
  - `daily_report_morning` の `expect_delivery=False`


---

## Tracker Integrity 修復（21件 → 0件）

### 発見
- `audit_trades_with_market_data.py` で integrity issue 21件検出
  - qty ミスマッチ（AMAT, ANET, PANW, QCOM, SNOW 等）
  - tracker-only symbols（ADBE, AMD, ARM, AVGO, CDNS, CRM, MDB, MRVL, MSFT, PATH, PLTR, SMCI, TSM 等 13件）

### 対応
1. dry-run 確認 → open 37件が整合的に再構築されることを確認
2. `rebuild_pnl_state_from_broker.py --backup` 実行
   - バックアップ作成済み
   - peak_price を backup から復元
   - 50 field(s) restored
3. Post-rebuild integrity check → **issue count: 0** ✅
4. `./console/manage.sh restart` でキャッシュクリア
5. テスト: `tests/test_console_services.py` → **19/19 PASSED** ✅

