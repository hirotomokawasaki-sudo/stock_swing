# 2026-08-04 デイリーログ

## 1. 🚨 Circuit Breaker HALT 発生・恒久修正・クリア（SNOW BUY qty lag）

### 発見の経緯

OpenClaw ダッシュボードから「現在対応が必要な問題はありますか」との問いに応じて定常監査を実施したところ、
`data/guardrails/circuit_breaker.json` が `08-03 19:55 JST` から `halted` のまま放置されていることを発見。
manual clear がない限り `apply_decision()` は絶対に halted から自動復帰しない実装のため、
このまま気づかなければ 08-04 22:25 JST の premarket 以降、全 cron で新規 BUY が停止するところだった。

### 根本原因

- SNOW は既に 116株のオープンポジションを保有していた
- 08-03 19:55 JST run で同銘柄にさらに 116株の BUY を追加発注
- トラッカーは発注と同時に qty を 232 に即時合算するが、broker 側の position qty は
  Alpaca の fill確認 + API伝播が完了するまで数秒ラグがある
- postrun の broker/tracker 整合性チェックがこのラグの最中（fill完了の約4秒前）に走り、
  `broker_qty=116` vs `tracker_qty=232` の qty_mismatch を検知 → `broker_tracker_mismatch`
  ルール（閾値1件でcritical）が発火 → 即HALT
- これは G1-v2（BUY presence lag）/ G1-v2-b（SELL qty lag）/ G1-v2-c（fast-fill SELL phantom）
  と同系統だが、「既存ポジションへの追加BUYによるqtyラグ」という**未対応の第4パターン（G1-v2-d）**だった

### 検証

- ブローカーへ直接問い合わせ、現在は broker側もqty=232でトラッカーと完全一致（全7銘柄でmismatch=0）を確認
- 誤検知であることを確認した上で `scripts/clear_circuit_breaker.py --confirmed-mismatch-count 0` で手動クリア

### 恒久修正（commit `f61586b`、push済み）

- `src/stock_swing/guardrails/postrun_mismatch.py`: `apply_lag_exclusion()` に G1-v2-d ルール追加
  - qty_mismatches のうち、symbol が今回run の new_buy_symbols に含まれ、かつ
    `tracker_qty > broker_qty`（トラッカーが先行している方向）の場合のみ除外
  - 逆方向（`broker_qty > tracker_qty` の BUY symbol）は別の実問題として除外しない
- テスト: `TestG1V2dBuyAddToExistingPositionQtyLag`（8件、SNOW再現テスト含む）追加
- 既存テスト1件（`test_buy_symbol_qty_mismatch_not_excused`）を broker-ahead 方向のテストに更新
  （新ルールで正当に除外される方向の値を使っていたため）
- フルテスト: 1276 passed, 2 skipped（既存の無関係な2件の失敗は本修正前から存在すること確認済み）

### 今後の確認事項

- 次回、既存ポジションへの追加BUYが発生した際、実際に false HALT が再発しないか確認
- `docs/console_improvement_tasks.md` の Codex Review 系ロードマップへの反映は次回整理時に実施

---

## 2. Circuit Breaker HALT の可視化改善（commit `d773b30`）

上記インシデントが約13時間発見されなかった原因として、メインコンソール（`/api/dashboard`
の `alerts`）に circuit_breaker の HALT が一切表示されていなかったことが判明。
モバイル read-only monitor（別トークン必須）と ConsoleRenderer のテキストバナー
（paper_demo を手動実行して stdout を見ない限り見えない）だけが状態を表示していた。

- `console/services/dashboard_service.py`: `get_alerts()` に `data/guardrails/
  circuit_breaker.json` を直接読む `_check_circuit_breaker_alert()` を追加。
  HALTED → critical alert（`guardrail_halted`）、RECOVERY_PENDING → warning alert
  （`guardrail_recovery_pending`）としてメインダッシュボードのアラート一覧に表示。
- テスト12件（`test_dashboard_circuit_breaker_alert.py`）: 正常系/ファイル欠損/
  破損JSON/境界値/`get_alerts()`への伝播（表示される・されない両方）を確認。

## 3. Finnhub news collection の `no_company_news` 誤判定を修正（commit `baa473c`）

- 07-30以降ほぼ毎回失敗していた `stock_swing_news_collection` の原因を修正。
  RBRK は該当期間に企業ニュースが実際に0件（API呼び出しは正常成功）だったが、
  これを `rate_limit`/`auth_error`/`timeout` などの本物の失敗と同一視して
  coverage breach と判定していた。
- `collect_data.finnhub_news_row_succeeded()` を新設し、`collect_data.py`（cron
  の成否判定）と `system_adapter.py`（コンソールの source_sla ヘルスチェック）
  で共有。`no_company_news` は成功扱い、他の失敗理由は従来通り失敗として扱う。
- テスト10件追加（`test_source_lineage.py` 7件 + `test_system_adapter_finnhub_
  source_sla.py` 3件、新規）。既存テスト2件のフィクスチャを `no_company_news` →
  `rate_limit` に修正（本物の失敗ケースを検証する意図を保つため）。

## 4. `capture_broker_tracker_snapshot.sh` のインポートパス修正（commit `2025110`）

- 07-29から `No module named 'stock_swing.brokers'` で毎回失敗していた根本原因を修正。
  存在しない `stock_swing.brokers.broker_factory.create_broker_client` を import
  していたのが原因（正しくは `stock_swing.sources.broker_client.BrokerClient` を
  `paper_demo.py` と同じ方法で直接構築する）。
- 実行確認: exit code 0、mismatch_count=0（全7ポジション一致）。証跡ファイルは
  `data/audits/broker_tracker_snapshots/snapshot_20260804_001647.json` に保存済み。

## 5. フルテストスイート確認（中間時点）

- 1301 passed, 2 skipped（既存の無関係な2件の失敗のみ、本日の変更前から存在することを
  都度 git stash で確認済み）。

## 6. TradingタブのDecisionsカウントが07-13リビリドに固定されていた（ユーザー報告、commit `6c06503`）

**ユーザー報告**: 「コンソールの取引タブのDECISIONSの数値はずっと500のままな気がする」

### 根本原因
- `DashboardService._load_recent_decisions()` は `data/decisions/*.json` を
  **ファイルmtime**でタブレし、上位500件に切り損ててから内容を読んでいた
- 07-13の一括 rebuild/backfill 作業が旧いdecisionファイルの**mtimeだけ**を
  一括で更新（内容はそのまま）したため、1956件がmtimeソートの上位を永久に
  占拠してしまい、これ以後の全ダビーとも 07-13時点の旧いスナップショット（最新が
  2026-06-30 CIENの決定を含む状態）を返し続けていた（2187件中の500件が常に同じ
  旧グループ）
- `funnel.decisions`の数値自体はmtime制度の仕様上500で上限だが、**内容が固定されていたこと**が本当のバグ

### 修正
- `_load_recent_decisions()` を全ファイル読み込み後、各決定の実際の時刻（generated_at/
  created_at/decision_time、なければファイル名推定）でソートする方式に変更。
  今後何があっても（rebuild/rsync/backup等）mtimeノイズに影響されない
- `console/ui/app.js`: タイルラベルを「Decisions」→「Decisions (直近500件)」に変更（
  仕様上の上限であることを明確化）

### 検証
- 修正前: ウィンドウ内の最新決定が 2026-06-30 CIEN（07-13 rebuildノイズ）
- 修正後: ウィンドウ内の最新決定が本日 2026-08-03 SNOWの決定（正しく最新反映）
- テスト6件追加（小規模で同一のmtime/contentスクーを再現する回帰テスト含む）

## 7. 最終フルテストスイート確認

- 1316 passed, 2 skipped（既存の無関係な2件の失敗のみ）。コンソール再起動して実機確認済み。
