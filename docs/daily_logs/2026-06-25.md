# 2026-06-25

## Integrity Repair + Root Cause Fix

### 1. Issue detected (09:23 JST)
- `audit_trades_with_market_data.py` で integrity issue 3件を検出
  - Broker positions: 13
  - Tracker open symbols: 13 (数量一致だが内訳に問題)
  - **CRDO: tracker qty=288 vs broker qty=85**（主要問題）
  - KLAC, MU: 価格差のみ（数量一致）

### 2. 即時修復
- `python scripts/rebuild_pnl_state_from_broker.py --backup`
  - Backup: `data/tracking/pnl_state_backup_20260625_092454.json`
  - CDNS qty mismatch（fills=79 → broker=128）を broker truth で修正
  - peak_price 17件を backup から自動復元
  - Post-rebuild: 17 open positions

- Post-rebuild audit: **Integrity issue count: 0** ✅

### 3. 根本原因調査

**Partial Fill + used_sell_order_ids バグ**

タイムライン（米東部時間 6/24）:
1. **16:00:20 ET**: paper_demo が sell 254 CRDO を送信
2. **16:00:21 ET**: ブローカーが 51株を即時部分約定
   → paper_demo インライン reconciler が 51株クローズを記録
   → `exit_broker_order_id = c0c0df3d` を保存
3. **その後**: ブローカーが残り 203株を同一注文で約定
4. **cron reconciler 実行時**:
   - `used_sell_order_ids` に `c0c0df3d` が存在 → **スキップ**
   - 203株が永久に記録されない
5. **19:55 ET**: 新規 buy 85 CRDO
   → tracker open = 35+84+84+85 = **288株**（実態 85株）

**コードの構造的欠陥**:
```
used_sell_order_ids にブローカー注文IDを追加
  ↓
同一注文の部分約定→完全約定でも「既処理」とブロック
  ↓
残りの株数クローズが永久に記録されない
```

### 4. 恒久修正実装（commit: b1c1726）

**`src/stock_swing/cli/reconcile_orders.py`**:

- `used_sell_order_ids` チェック時に partial fill completion ロジックを追加
- `recorded_qty_by_order_id` map（注文ID → 記録済み株数）を事前構築
- 注文IDが `used_sell_order_ids` に存在する場合:
  - `broker_filled_total - already_recorded = remaining` を計算
  - remaining > 0 なら **スキップせず残りを処理**
  - remaining ≤ 0 なら真の重複 → スキップ
- `effective_fill_qty` (remaining or full qty) を `record_exit` に渡す
- 記録後に `recorded_qty_by_order_id` を更新（同一ランン内の重複防止）

**テスト追加**:
- `test_partial_fill_completion_records_remaining_qty`: 51株記録済みの状態から 203株が正しく追記されることを確認
- `test_partial_fill_already_complete_is_not_replayed`: 完全記録済みの場合は再実行されないことを確認

**テスト結果**: 5/5 PASSED（全体 500/501 passed、既存失敗 1件は別問題）

### 5. CDNS の再発パターンについて

同様の partial fill バグにより、6/23 rebuild 後に CDNS (fills=79, broker=128) の +49株差が再発している可能性が高い。今回の修正で自動的に修復される見込み。

### 6. Summary
- Rebuild + audit で完全修復 ✅
- 根本原因（partial fill tracking バグ）を恒久修正 ✅
- Console 再起動でキャッシュをクリアすること

---

## 改善計画 大規模実装デー（09:40〜11:30 JST）

### 概要
外部 AI レビューに基づき改善計画を R0-R8 にゼロベースで再構成し、本日中に R0・R1-A・R2-C・コンソール C1+C2 を実装した。

### コミット一覧

| commit | 内容 |
|---|---|
| `460049f` | feat(P1-P5): KLAC incident replay guard, stale fill defense, run context, walk-forward exit, correlation cluster, console summary, AI context packs, secret scan, signal strength save |
| `2fb494f` | feat(P6+P9): experiment management + autonomous stop guards |
| `ce3f83e` | docs: 改善計画更新（P6/P9 完了マーク、P16/P17 追加） |
| `9ac0851` | docs: zero-based plan overhaul → R0-R8 roadmap（外部 AI レビュー反映） |
| `89f8ed7` | feat(R0+R1-A+R2-C): paper_demo に ExperimentContext + Guardrail 接続、exit ログ、個別株サイズ 0.5x |
| `a2057e7` | docs: R0-R8 スケジュール更新（R0/R1-A/R2-C 完了マーク） |
| `b27716e` | feat(console C1+C2): structured alerts, text renderer, price integrity, API/Token monitor |
| `6c6dd1a` | docs: console C-batch を R6 スケジュールにマージ |

### 実装内容

**R0（安全モジュール統合）**
- `ExperimentContext` + `ExperimentRegistry` を paper_demo に接続
  → 全 run に `experiment_id` / `config_hash` / `source_commit` が付与される
- `GuardrailEngine` + `CircuitBreakerStore` を paper_demo に接続
  → startup / buy-gate / post-run の3ポイントでガード
  → `paper_warning_only: true`（warning log のみ、hard-halt は 2週間後に有効化）
- `data/guardrails/` ディレクトリ作成

**R1-A（exit シグナル発火診断）**
- `simple_exit_v2_strategy.py` に `exit_check` DEBUG / `exit_signal_fired` INFO ログを追加
- `paper_demo.py` に `exit_signal_generated` / `exit_signals_none` ログを追加
- 次の本番 run で Case A（シグナル未発火）or B/C/D（reason 消失）を判定可能

**R2-C（個別株サイズ縮小）**
- `STOCK_POSITION_SIZE_MULTIPLIER = 0.5`（env var でオーバーライド可）
- ETF は `0.70` のまま変更なし
- PF=0.777 の個別株リスクを今日から半減

**Console C1+C2**
- `console_summary.py` 全面強化: `ConsoleAlert` / OK-DEGRADED-HALTED 判定 / `save_json()`
- `console_renderer.py` 新規: 6セクションのテキスト表示
  - RUN HEALTH（✅/⚠️/🚨 + experiment_id + guardrail 状態）
  - ALERTS（severity 順ソート）
  - PORTFOLIO（equity / PnL / open positions）
  - PRICE INTEGRITY（fresh/stale/fallback + sources breakdown）
  - DECISION FUNNEL（candidates → buy/sell/deny → submitted）
  - API / AI COST（p50/p95 latency + token 消費）
- 毎 run `reports/console/latest_console_summary.json` にアトミック保存

**テスト**
- `test_runtime.py` を tmp_path ベースに修正（既知の失敗を解消）
- **499 passed, 0 failed**（↑ 475 → 499）

### 改善計画の状態

| フェーズ | 状態 |
|---|---|
| R0 | ✅ 完了（warning_only 期間中） |
| R1-A | ✅ 完了（次 run でログ確認が必要） |
| R2-C | ✅ 完了 |
| R6 C1+C2 | ✅ 完了 |
| R1-B〜D | 🔲 明日以降（Case 特定後） |
| R2-A/B/D | 🔲 06-28〜07-04 |
| R6 C3 | 🔲 07-07〜07-14 |

### 次のアクション
1. 次の本番 paper_demo run（今夜 15:55 ET = 明日 04:55 JST）のログで `exit_signal_fired` を確認
2. Case 特定 → R1-B（exit_reason ライフサイクル修復）に着手
3. 07-09 頃に Guardrail warning ログの頻度を確認し閾値を調整
