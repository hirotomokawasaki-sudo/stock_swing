# 2026-07-13 作業ログ

## 朝の状態確認

| 項目 | 状態 |
|---|---|
| Cron 12本 | 全 ok |
| Reconcile（本日実行済み）| open=20, closed=199, エラーなし |
| 累積実現 PnL | -$93,583.16 |
| open_trades | 20件 |

## バグ修正: sector_shock_hold `no_sector_data`

### 根本原因（2箇所）

1. **`PriceMomentumFeature`** が `return_1d`（前日比リターン）を `values` に保存していなかった
   - `values` には multi-day `momentum` のみ存在
   - `return_1d` = `(latest_close - prev_close) / prev_close` を last 2 bar から計算するよう追加

2. **`paper_demo.py`** のルックアップが存在しない属性を参照していた
   - `getattr(_feat, "return_1d", None)` → `FeatureResult` に直接属性なし
   - `getattr(_feat, "payload", {}).get("return_1d")` → `payload` 属性なし（`values` が正しい）
   - 修正: `_feat.values.get("return_1d")` に変更

### 結果

- `_sector_1d` が常に `{}` → `classify()` が `no_sector_data` にフォールバック
- 修正後: SMH/SOXX の実際の 1d リターンが渡され、sector_shock_hold / soft_stop の正確な判定が可能に

### テスト

- `test_price_momentum_feature_return_1d` を新規追加
- 全テスト: **663 passed, 2 skipped** ✅

### Commit

`c275fa5` — fix: sector_shock_hold no_sector_data — add return_1d to PriceMomentumFeature + fix paper_demo lookup

## 次の確認事項

- 次回 paper_demo 実行ログで `sector_shock_hold SHADOW` が `no_sector_data` でなく `soft_stop` / `sector_shock_hold` を出力するか確認

---

## RF-8b: attribution coverage 100% 達成

### 問題の根本原因（2バグ）

| バグ | 内容 | 修正 |
|---|---|---|
| Bug 1 | decision `generated_at`（生成日 T）と `exit_time`（執行日 T+1〜4）がズレ、`(symbol, exit_date)` が一致しない | `LOOKBACK_DAYS=10` で exit_date から最大 10日遡って照合 |
| Bug 2 | `"Max hold period reached"` がキーワード不一致で decision_map に入らない | `"max hold"` キーワード追加 → `time_based` |

### 回収結果

| ソース | 件数 |
|---|---|
| decision JSONs（日付遡及） | 63件 |
| 手動注記（broker API 確認） | 6件 |
| **合計** | **69/69件（100%）** |

### 手動注記の根拠

- AVGO×3, SMH, SMHX（05-13〜15）: 15:30 UTC に market sell → 市場オープン同時 stop_loss パターン
- CRWD 07-06: `client_order_id=crwd-split-fix-20260703` → 4:1 split 修正時の手動売り → `corporate_action`

### 最終 attribution（clean 199件）

| reason | n | WR | PF | net PnL |
|---|---|---|---|---|
| stop_loss | 88 | 25.0% | 0.069 | -$150,837 |
| trailing_stop | 68 | 85.3% | 25.873 | +$124,669 |
| breakeven_stop | 33 | 27.3% | 0.696 | -$4,417 |
| time_based | 9 | 88.9% | 6.578 | +$24,277 |
| corporate_action | 1 | 100% | ∞ | +$617 |

### Commits

- `c275fa5` — sector_shock_hold no_sector_data バグ修正
- `5bdc09a` — RF-8b attribution coverage 100%
