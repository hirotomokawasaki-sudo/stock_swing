# stock_swing Daily Improvement Log

## Date
- 2026-08-07

## 今日着手したタスク
- 「現在対応が必要な問題があるか」の調査依頼を受け、console self-check の
  false "blocked" 表示を調査・修正
- NBIS高掴みインシデント（3件同時損切り、-$7,774）のレビューと戦略フィードバック
- 「今日対応可能な他の改善事項」調査 → Good Friday判定バグ発見・修正
- 「戦略検証に必要なデータで取得失敗しているものはないか、将来必要なデータはあるか」
  調査 → `event_swing_v1` が実装以来一度も動いていなかったことを発見、決算カレンダー接続で解消
- 自己レビューにより、本日実装したPlan B/C shadow logging が `--dry-run` テストで
  本番ログを汚染していたバグを発見・修正

## 実施内容

### 1. Console self-check false "blocked" 修正（commit `3be4d05`）
- `ledger_quality_gate.last_checked` が6日間stale（`check_go_no_go.py`は毎日再確認
  しているのにフィールド更新されていなかった）→ `--save`時に自動スタンプするよう修正
- `circuit_breaker.json` のfreshness判定が`cleared_at`/`triggered_at`（状態遷移時のみ更新）
  を見ていたため、健全にokが続く状態がstaleと誤判定 → `last_evaluated_at`ハートビートを
  毎回の評価で追加
- Finnhub source_sla coverage 86.4%（6/44失敗）→ retry設定強化
  （max_attempts 2→3, timeout 5s→10s）で0/44に解消
- テスト23件追加、フルスイート1372 passed

### 2. NBIS高掴みインシデント対応（commit `07ca7e0`）
- 2026-08-04〜06、37時間で同一銘柄に3回買い増し→3ロット同時損切り(-$7,774)を分析
- 段階的ロールアウトで対応（全部同時実装は非推奨と判断・ユーザー合意）:
  - **Plan A（同一銘柄24hクールダウン）**: active即時有効化。`same_symbol_cooldown.py`
  - **Plan B（ボラ上限ゲート）**: shadowモードのみ。`volatility_gate.py` +
    `finnhub_metric_lookup.py`。NBIS実データで正しく検知（3m_std=132.6%>cap=120%）
  - **Plan C（52週高値乖離診断）**: observability-onlyのみ。`distance_from_high.py`。
    NBIS実データで正しく検知（-36.7% below high, momentum+11.9%）
- テスト69件追加、フルスイート1441 passed
- 2026-08-14（1週間shadowログ確認）・08-21（中間レビュー・昇格判断、要ユーザー承認）
  をcron登録

### 3. Good Friday判定バグ修正（commit `eb4caad`）
- `MarketCalendar.is_us_holiday()`のGood Friday近似ロジック（4月15〜22日の金曜）が
  実際のGood Friday（イースター-2日）と一致するのは6年に1回程度
- 2026年（4/3）・2027年（3/26）・2028年（4/14）・2029年（3/30）は休場日として
  検知できていなかった → 実害あり（次のGood Friday=2026-04-03でpaper_demoが
  通常営業日として動作してしまう状態だった）
- Anonymous Gregorian算法で正確なイースター計算に置き換え、テスト9件追加
- ドキュメントの陳腐化記載（Massive connection pool、実は07-23に修正済み）も訂正

### 4. データ取得状況調査 + event_swing_v1 決算カレンダー接続（commit `b5fc1c4`）
- 現在の取得失敗: 実質ゼロ（本日のretry強化が効き、Finnhub/Massive/broker全て健全）
- **重大発見**: `FinnhubClient.fetch_earnings_calendar()`は実装済みだが一度も
  呼ばれておらず、`EventSwingStrategy`（event_swing_v1戦略）が全2,306件の
  decisionファイル中**0件**という、実装以来一度も動いていない状態だった
- `collect_data.py::collect_earnings_calendar()`新規実装（1回のAPI呼び出しで
  全カレンダー取得→ローカルでユニバースにフィルタ）、`paper_demo.py`に配線
- 実APIコールでE2E確認: 1418件→3件（AMAT/NBIS/SMCI）に正しく絞り込み、
  `event_swing_v1`が初のBUY信号（NBIS, strength=0.65）を生成することを確認
  （Plan Aのrolling PF gateで正しくブロックされることも確認）
- 他の未接続データ（企業ニュース、インサイダー取引、Filing sentiment、
  Massive SMA/RSI、FRED マクロ）と将来必要なデータ候補（オプションフロー、
  ショートインタレスト、セクター時系列ボラティリティ）をdocs化
- テスト15件追加、フルスイート1465 passed
- 2026-08-14（event_swing_v1初週間レビュー）をcron登録

### 5. 自己レビューで発見: dry-runによる本番shadow log汚染修正（commit `3ae4115`）
- 本日実装したPlan B/C shadow loggingコードに`--dry-run`ガードが漏れていた
- 実際に本日のdry-runスモークテスト4件が`data/volatility_gate_shadow_log.jsonl`
  と`data/distance_from_high_log.jsonl`を汚染していたことを検出・確認
  （タイムスタンプが実cron実行時刻と一致せず、手動テスト時刻と一致）
- 08-14/08-21のレビュー判断がこのログの偽陽性率に依存するため、汚染は看過できない
  問題と判断し即座に修正
- `if decision.action == "buy" and not args.dry_run:` にガード追加、汚染済み
  4エントリを削除（クリーンな状態から再蓄積開始）
- ミューテーションキラーテスト3件追加（実際にガードを外すとテストが失敗することを
  確認済み）、フルスイート1468 passed

## 確認結果
- console watchdog: 全commit後の再起動を経てもhealthy継続確認済み
- git status: `data/`配下の日次収集データ・cronによる自動更新分のみ未コミット（想定内）
- 本日の全変更: commit `3be4d05` → `07ca7e0` → `eb4caad` → `b5fc1c4` → `3ae4115`
- テスト: 1372 → 1468 passed（既知の無関係な2件の失敗は変更前から再現、不変）

## ブロッカー
- なし（発見した問題は全て同日中に修正・検証完了）

## 次アクション
- 2026-08-14: R9 Plan B/C shadowログ1週間チェック + event_swing_v1初週間レビュー（cron）
- 2026-08-21: R9 Plan B/C 中間レビュー・昇格判断（要ユーザー承認、cron）
- 2026-08-19頃: R3-v2-Stop（tiered min_hold v2）中間レビュー（既存予定）
- 2026-08-20以降: リアルトレード移行判断（Go/No-Go要件は維持、7/7 GO継続）
- 今後のフォローアップ候補（優先度順）: ニュースセンチメントのトレード接続、
  Massive SMA/RSI接続、インサイダー取引/Filing sentiment接続

## 反映確認
- [x] API（各コード変更を実データ/実APIコールで動作確認済み）
- [x] UI（console self-check修正、watchdog healthy確認済み）
- [x] tracker / broker truth（変更なし、既存整合性維持）
- [x] logs / cron（新規cron 4件登録、既存cron 1件のsources更新）
- [x] テスト（全変更にmutation-killer含むテスト追加、フルスイート実行済み）
