# stock_swing Daily Log - 2026-05-28

## 作業概要

本日は朝から発生していた複数のコンソール表示バグと運用経路の問題を一括修正。  
今後の改善計画の整理も実施。

---

## 実施内容

### 1. PnL State 修復（09:05 JST）
- **事象**: Broker 38 positions / Tracker 38 → integrity issue count 0（正常）
- **確認**: `audit_trades_with_market_data.py` 実行 → all clear
- **副作用発見**: rebuild スクリプトが `daily_snapshots`（4件）と全53ポジションの `peak_price` を消失させていた

### 2. daily_snapshots / peak_price 復元（09:20 JST）
- backup `pnl_state_backup_20260528_090535.json` から復元
  - `daily_snapshots`: 0 → 4件
  - `peak_price`: 52/53 ポジション復元、QCOM は entry_price で初期化
- rebuild スクリプトのコードは正しいが、次回 rebuild 後も要確認を継続

### 3. コンソール表示バグ修正（09:20〜09:48 JST）

#### Bug 1: live_metrics の current_equity が $10万・positions_count が 0
- **原因**: `get_live_metrics()` が `positions.get('current', [])` を参照していたが、`get_positions()` は `'positions'` キーで返していた（キー名ミスマッチ）
- **修正**: `positions.get('positions') or positions.get('current', [])` に変更
- **結果**: equity $1.034M・drawdown 2.26%・risk MODERATE が正常表示

#### Bug 2: 取引タブの direct_recent が古いトレード（5/12〜5/15）を表示
- **原因**: `get_recent_trades()` が trades 配列の末尾10件をそのまま取得していた。rebuild で追加された古い broker_reconstructed トレードが配列末尾にあったため
- **修正**: `exit_time` でソートしてから末尾10件を取得するよう変更
- **結果**: 直近 Sell（ARM/QCOM/MRVL/MDB @5/27）が正しく表示

### 4. reconcile cron 改善（09:20 JST）
- **reconcile delivery を announce に変更**: 従来 `delivery.mode: none` のためサイレント失敗が検知できなかった → Telegram announce に変更
- **sell 誤マッチ修正**: シンボル名のみの突合 → ログ timestamp ±10分以内の broker order を優先マッチする方式に変更
- **stale pending_exit_reasons 自動クリーンアップ**: paper_demo が直接記録した exit について reconcile 実行時に自動削除（ARM/MDB 2件即時クリーンアップ確認）
- **scripts/reconcile_orders.py**: placeholder → 本物 CLI モジュールへのプロキシに変更

### 5. commit & push（09:48 JST）
- commit: `a97d557`
- 対象: 4ファイル変更（dashboard_service, reconcile_orders×2, pnl_tracker）

### 6. rebuild post-check 自動化（10:16 JST）
- **背景**: rebuild 後に `daily_snapshots`（4件）と `peak_price`（53件）が毎回消える問題が再発リスクとして残っていた
- **実装**: `scripts/verify_rebuild_integrity.py`（新規）
  - `daily_snapshots` が空になっていないか（バックアップと件数比較）
  - 全 open trade に `peak_price` が設定されているかを確認
  - `--fix` フラグで最新バックアップから自動復元（FIFO lot マッチング、なければ entry_price で初期化）
- **rebuild スクリプトへの組み込み**: `rebuild_pnl_state_from_broker.py` の末尾に post-check を自動サブプロセス実行を追加
  - バックアップあり → `--backup <path> --fix` で自動修復
  - 問題検出時は `⚠️` を表示して修正内容を出力
- **動作テスト**: 意図的に `daily_snapshots=0` / `AMAT peak_price 削除` を注入 → 2件検出・自動復元 → 再チェック `✅` を確認
- commit: `ec10f82` → push 済み

---

## 確認結果

| 項目 | 状態 |
|---|---|
| Broker/Tracker 整合性 | ✅ 38/38 完全一致、integrity 0 |
| live_metrics equity | ✅ $1.034M（修正前は $10万誤表示） |
| 取引タブ recent_trades | ✅ 直近 Sell が正しく表示 |
| reconcile cron | ✅ Telegram announce + timestamp マッチに改善 |
| peak_price | ✅ 全53ポジション復元済み |
| daily_snapshots | ✅ 4件復元済み |
| rebuild post-check | ✅ `verify_rebuild_integrity.py` 実装・自動 fix 動作確認 |

---

## 今日判明した重要課題

### rebuild スクリプトの fragility
- rebuild 後に daily_snapshots / peak_price が消える事象が今朝再発
- コード上は保存しているはずだが、実際には消えた
- **次アクション**: rebuild 実行後の post-check スクリプト追加（daily_snapshots と peak_price の有無を自動検証）

---

## 次アクション候補

### 今週中（優先）
1. **T15/T20 監視継続**: 今夜 22:30 JST の paper_demo cron 正常完走確認
2. **Profit Factor 再計測**: 5/27 exit 強化後の最初のトレードが出たら実施

### 〜6/15（計画）
4. **T25 Step 1**: `analyze_news_impact.py` で株式44銘柄の感情相関評価
   - `scripts/analyze_news_impact.py` は既に存在している

---

## 午後作業（16:00〜17:00 JST）

### 7. Console Fetch Stability — Batch 1〜2 実装（16:00 JST）

**背景**: ブラウザ側で頻発していた `Failed to Fetch` の根本対策。

#### 実装内容

| ファイル | 変更内容 |
|---|---|
| `src/stock_swing/storage/atomic_json.py` | **新規** — `atomic_write_json` / `read_json_with_retry` |
| `src/stock_swing/tracking/pnl_tracker.py` | `_save_state` → atomic write、`_load_state` → retry read（5回）|
| `src/stock_swing/tracking/exit_reason_store.py` | 全 read/write を atomic 版に置換 |
| `console/services/response_cache.py` | **新規** — スレッドセーフ TTL キャッシュ |
| `console/services/safe_file_reader.py` | **新規** — last-known-good フォールバック付き JSON リーダー |
| `console/app.py` | `HTTPServer` → `ThreadingHTTPServer`、RLock、TTL キャッシュ、`_timed_json`（timing + request_id）|

**テスト**: 37/37 PASSED

#### 解決した問題
1. シングルスレッド → `/health` が dashboard 処理中でも応答するように
2. 非 atomic な JSON 書き込み → `tempfile + os.replace` で部分書き込みを排除
3. トランジェント読み取りエラー → リトライ + last-known-good で HTTP 500 を抑制
4. 高頻度ポーリング重複計算 → TTL キャッシュ（dashboard 10秒、positions 8秒 等）
5. デバッグ困難 → 全 API ルートに `request_id` + `duration_ms`、2秒超は `WARN slow_api_route`

---

### 8. Analytics Batch 1 実装（16:30 JST）

**背景**: バックログ `NEXT_IMPROVEMENT_BACKLOG.md` P1 4件の検証・実装。

#### 実装内容

| モジュール | 内容 |
|---|---|
| `src/stock_swing/analytics/strategy_attribution.py` | ETF/Stock/戦略/exit_reason 別 PF・損益分析 |
| `src/stock_swing/analytics/data_quality_audit.py` | signal_strength 欠損・price override・quality_flags 検出 |
| `src/stock_swing/risk/risk_budget.py` | 総・銘柄・セクター・ETF 別リスク予算計算 + buy 候補拒否ロジック |
| `src/stock_swing/analytics/exit_replay.py` | 7種類の exit ポリシー比較（tighter stop / breakeven / trail / time stop 等）|

#### コンソールエンドポイント追加（`console/app.py`）

| エンドポイント | TTL | クエリパラメータ |
|---|---|---|
| `GET /api/strategy_attribution` | 30秒 | `?dimensions=asset_class,strategy_id` |
| `GET /api/data_quality` | 20秒 | なし |
| `GET /api/risk_budget` | 15秒 | なし |
| `GET /api/exit_replay` | 60秒 | `?policies=tighter_stop_4pct,...` |

**テスト**: 51/51 PASSED（+ コンソール安定化テスト含め合計 60/60 PASSED）

#### 実データでの確認結果

| 指標 | 値 |
|---|---|
| ETF PF | 0.168（バックログ仕様値と完全一致）|
| Stock PF | 1.731（同上）|
| open positions missing signal_strength | 53/53 件（全欠損、保守的閾値適用中）|
| price overrides アクティブ | 4 シンボル |
| 現在の total open risk | $68,144 / equity の **6.6%**（ポリシー上限 3% の 2.2倍）|
| exit replay: best scenario | `tighter_stop_4pct` で +$1,012 改善 |

#### 今後の注意事項
- **Risk Budget の閾値**: open risk 6.6% はポリシー上限 3% を大幅超過。paper_demo への deny ロジック組み込み前に閾値チューニング必須
- **Exit Replay の精度**: 全 85 closed trades で `peak_price` が欠損（broker_reconstructed トレードのため）→ 2026-05-27 以降の新トレードからのみ有効
- **P0 safety 変更なし**: ETF buy ブロック・missing price fallback 禁止・stale data 検知は全て維持

---

## ブロッカー
- なし

## Equity（本日 09:00 JST 時点）
- Equity: **$1,030,370** (+3.0% from $1M initial)
- Open positions: 53トレード / 38銘柄
- Unrealized PnL: +$54,575
