# 2026-09-05 作業ログ

## P1: early weakness cut ヒストリカル検証（R13-A2、ユーザー承認済みタスク1/2）

ペーパー実績（closed 357件、実現PnL -$38,253 / PF 0.888）の主力出血源が
「保有5日以上のstop_loss（57件 -$125.6k）」であることを受け、反実仮想ルール
「保有K日目以降、最高含み益+2%未満 かつ 現在リターン-3%以下なら終値でexit
（ハードストップより先に判定）」をK∈{3,4,5}×3コホートでヒストリカル検証。

- `scripts/simulate_early_weakness_cut.py` 新規作成（読み取り専用）。
  R13-Aの `simulate_stop_loss_deepening.py` と同一の日次パスリプレイ手法
  （`SimpleExitV2Strategy` 自身のメソッドでexit優先順位を再現、価格取得も同一）を流用。
- 結果: **全closed（n=295）ではK=3/4/5すべて明確に悪化**（-$34k〜-$85k、
  機会損失が支配的: AMD逸失$25k・PANW $14k等、K=3で25件$146k）。
  attributable限定（n=43）のみ全Kで小幅プラス（最良K=4 +$1,849、PF 1.031→1.091）
  だが小標本・母集団依存（R13-Aと同一パターン）。
- 判定: 昇格を後押しする頑健なエビデンスなし。**paper A/B昇格はユーザー判断待ち**。
  実装・config変更なし。
- エビデンス: `docs/r13_early_weakness_cut_validation_20260905/`
  （summary.md + 生出力 + per-trade JSON）
- docs: `console_improvement_tasks.md` にR13-A2 subsection追記（R13-A直後）。

## P0: Go/No-Go 経済性ゲート追加（ユーザー承認済みタスク2/2）

`scripts/check_go_no_go.py` に必須チェック **economic_viability** を追加。
従来のRequired条件は全て「壊れていないか」の検査で、「儲かっているか」を問う
条件が存在しなかったギャップを塞ぐ。

- コホート: `data/tracking/pnl_state.json` の closed トレードで
  exit_time >= 2026-08-14（`--econ-cohort-start` で上書き可）。
- 合格条件: n>=30 かつ PF>1.0 かつ expectancy>0。n<30 は insufficient_sample
  として fail-closed（NO-GO）。
- レポートにRequired行 + 「経済性ゲート詳細」セクション（R5-v2 Promotion Gate
  補足と同形式）を追加。
- 実行結果（2026-09-05 04:18 JST、--save済み → `docs/go_no_go_result_20260905.md`）:
  **NO-GO**。economic_viability: n=45 / PF=0.530 / expectancy=-$551.33 で❌
  （他のRequired 9条件は全て✅）。**これは意図された結果**（ユーザー承認済み
  fail-closed設計。PF<=1.0の間はGOを出さない。閾値の緩和は不可）。
- テスト: `tests/unit/test_check_go_no_go_economic.py` 新規（15件）。
  既存の all-green fixture（`test_check_go_no_go.py`）に経済ゲート合格分の
  tradesを追加。関連5ファイル計65テスト全緑。
  ※依頼文の `tests/test_check_go_no_go_promotion.py` 等はリポジトリ実体では
  `tests/unit/` 配下のため、同ファイルを `tests/unit/` パスで実行。

## R13-B1: ss縮小サイジング反実仮想（ユーザー承認済み・読み取り専用分析1/3）

`scripts/analyze_ss_sizing_counterfactual.py` 新規（読み取り専用）。
entry_signal_strength付きclosed 132件を対象に、バンドB∈{[0.85,1.00),[0.88,1.00),
[0.90,1.00)}×縮小率f∈{0.5,0.0}（ss==1.00ちょうどはフルサイズ維持）でPnL×fの
一次近似反実仮想を実施。

- 結果: **[0.85,1.00)のみ実質改善**（全期間 f=0で+$18,968、PF 0.461→0.507、
  直近exit>=07-16コホートでPF 0.562→0.643）。ドライバーはFTXL/CHPX（08-24半導体
  セルオフ）等の負け縮小で、セクター集中問題と交絡。[0.88/0.90,1.00)はNet微増だが
  PF悪化（HPQ +$4,199の機会損失が拮抗）。影響最大14件の少数トレード依存。
- ⚠️ 前提数値の食い違いを検出: 引用されていた「ちょうど1.00は+$23k」は現データで
  再現せず（ss==1.00はn=46/-$38.8k）。summary.mdに明記。
- 判定: 昇格を単独で後押しする頑健なエビデンスなし。**paper A/B昇格はユーザー
  判断待ち**。実装・config変更なし。
- エビデンス: `docs/r13b_ss_sizing_validation_20260905/`、
  tasksにR13-B1 subsection追記。

## セクター集中上限 反実仮想検証（ユーザー承認済み・読み取り専用分析2/3）

`scripts/analyze_sector_cap_counterfactual.py` 新規（読み取り専用）。
pnl_state.jsonから時系列ポートフォリオを再構成（notional=qty×entry_price、
equity=baseline $1M+実現累積）し、「エントリー実行後の同一セクター露出/equity >
cap∈{30,40,50}% ならブロック」を逐次シミュレーション+PnL一次近似で検証。
セクター分類は symbol_registry.yaml の sector（全58取引銘柄カバー、semis→
semiconductor正規化）+ is_semiconductor_related連結ビュー。

- 結果: **cap 30%のみ両コホートで改善**（全期間 +$31.5k/PF 0.888→0.975、
  直近exit>=07-16 +$12.9k/PF 0.634→0.733）。直近でブロックされるのは
  FTXL/CHPX（08-17エントリー→08-24セルオフ、-$12.8k）で既知の出血診断と一致。
  **capは非単調**（40%は-$4.6kと悪化＝勝ちを多く消す）。機会損失を明示
  （cap 30%でも勝ち30件+$39.3kを同時に消す）。半導体集中の実測ピークは
  equity比78.2%（06-04）。現在のopenは半導体関連3.7%で非抵触。
- R5-v2 cluster cap（`_filter_buys_by_cluster_cap()`閾値40%）の妥当性検証に
  直接接続。09-08 R13-D配線判断の材料。
- エビデンス: `docs/r13_sector_cap_validation_20260905/`、tasksにsubsection追記。

## 9/8レビュー判断基準の事前固定（ユーザー承認済みタスク3/3、docs整備のみ）

`docs/review_criteria_20260908.md` 新規作成。09-08に判断が集中する5件
（volatility_adjusted_stop中間レビュー / R16 per_lot_time_based_exit有効化 /
R14 dip-buy shadow初回レビュー / R13-D ETFセクターローテ配線判断 /
Plan C paper_ab昇格）の「継続/昇格/中止」数値条件を、当日のブレ防止のため
事前明文化。各cron payload・tasks該当節・過去daily logsと整合を確認済み。

- 総論: 経済性ゲート現在値（n=45/PF 0.530/expectancy -$551=NO-GO、09-05再確認）
  を全判断の前提とし、出血削減系は昇格検討可・エクスポージャー増系は保守側。
  JP半導体spillover（Phase 2.5）は半導体集中を増やす方向のため配線優先度低。
- vol_stop: 既存基準（n>=15〜20、widen悪化0件、tighten改善継続）+ 新観点として
  「直近コホート（exit>=08-14）のwiden/tighten全件列挙とbaseline（-5/-7/-9固定）
  比損益差の算出」を必須手順化。09-05時点 stop_loss & entry>=08-14 は n=11で未達。
- R16: 有効化条件（disagreement実害2件以上かつ累計-$4,000超等）は09-01
  インシデント（NOW/ORCL計-$5,101）で既に充足している事実を明記。フラグ変更は
  ユーザー承認必須。
- R14: 蓄積500件以上（09-05時点599件）・エラー0・分布妥当性の基準+観測継続期限
  10-02。シグナルが半導体銘柄に集中している点を注意事項として記録。
- R13-D: 配線条件を「--enforce-min-members結果のみ正式材料 + 小サイズ10%以下 +
  半導体関連連結露出をequity比30%以下に制限」で固定。**同日実施のセクター集中
  上限検証（cap 30%のみ直近改善）を判断材料に含めることを明記**。
- Plan C: 09-04クリア済み条件の再集計確認 + paper_ab（activeではない）形態 +
  トレードオフ提示 + ユーザー承認必須、の最終チェックリスト化。
- `console_improvement_tasks.md` の「次のアクション」冒頭からリンク。

## 日本市場拡張ロードマップ(総合版)の策定(ユーザー指示)

「今後は日本市場の株等も購入可能になる。戦略上筋のよい候補は」との問いに優先順位を
提示し、ロードマップ反映の指示を受けて `docs/jp_market_expansion_roadmap.md` を新設。
3トラック(①JPセクターETFローテ ②spillover一般化 ③決算PEAD)+大原則(負け戦略輸出
禁止・同一規律・半導体30%予算へのJP分合算)+共通ブロッカー(IBKR接続方式・マルチ通貨
PnL・単元株・禁止リスト配線)を明文化。既存 jp_semiconductor_ai_expansion_plan.md は
Track 2特化ケースへ位置づけ変更、Phase 3配線優先度を「低」に変更(セクター集中上限
検証の結果に基づく)。実装・設定変更なし(ドキュメントのみ)。

## R18起票 + ヒストリカル検証2本（ユーザー承認済みタスク、読み取り専用）

ギャップ分析で判明した構造的不足（レジームフィルタなし/決算ブラックアウトなし/
ATR定額サイジングなし）を `console_improvement_tasks.md` に **R18** として起票
（R18-A〜E + 備考: 非ITユニバース拡張・下落ヘッジ）。うち2本を同日検証:

- **R18-A レジームフィルタ（❌ 否定的→クローズ）**:
  `scripts/analyze_regime_filter_counterfactual.py`（SPY/VIX、前営業日close判定で
  look-ahead回避）。5バリアント中ブロック発生は SPY<50SMA 系のみ9件で、
  **PnL差 -$1,316（悪化）**。VIX>20該当日はほぼ4月（運用開始前）に集中し、
  7月の出血-$85.6kはリスクオン相場内で発生 → 指数レジームでは回避不能。
  深いベア相場での保険価値のみ判定保留。
  エビデンス: `docs/r18a_regime_filter_validation_20260905/`
- **R18-B 決算ブラックアウト（△ 中規模プラス、単独昇格なし）**:
  `scripts/analyze_earnings_blackout_counterfactual.py`（決算日=r11_earnings_cache、
  カバレッジ100%、event_swing系closed 0件）。N=7日で全期間**+$9,125**
  （PF 0.888→0.907）/直近コホート+$4,324（0.634→0.660）だが、N=3/5はほぼ無効で
  効果は4件の大負けに依存。**ギャップ貫通ストップ29件/-$111kのうち決算またぎは
  1件のみ（AVGO -$8.6k）** → ギャップの主因は決算ではなく、被弾額管理（R18-C）が
  構造的に有望という重要な副産物。
  エビデンス: `docs/r18b_earnings_blackout_validation_20260905/`

両summaryに多重検定注意（同一357件への反実仮想重ね掛け、昇格はR13-C後の
out-of-sample確認必須=R18-D）を明記。戦略実挙動・config変更なし。

## R18-D 発効: 昇格ガバナンスの必須条件化（ユーザー承認済み 06:27、タスク1/3）

`docs/promotion_governance.md` を新規作成し、R18-D を「起票のみ」→「発効済み」に更新。

- 反実仮想検証を**探索的**と位置づけ（同一closed 357件への重ね掛け=多重検定、
  累計8本・09-05だけで5本）。反実仮想の数値は単独では昇格根拠にならない。
- 本番反映（paper A/B昇格を含む挙動変更）の必須3条件を明文化:
  **(a) 判断基準の事前登録 (b) paper A/Bでの前向き確認 (c) R13-C完了後の
  out-of-sample再検証**（R13-C完了前に昇格したものは遡及再検証・不合格なら差し戻し）。
- **検証台帳（Verification Ledger）初版**を記載: 08-14 volatility_adjusted_stop /
  08-23 R13-A stop深掘り・R13-B ss exit切り離し（graduation）/ 09-05 early weakness
  cut・ss帯縮小・セクターcap・R18-A レジーム・R18-B 決算ブラックアウト の計8本。
  ※依頼文では graduation（ss exit切り離し）を「本日の6本」に含めていたが、リポジトリ
  記録上の実施日は 2026-08-23 のため、台帳には正確な日付で記載。
  今後の反実仮想は**当日中の台帳追記を必須**とし、追記なしの結果は昇格判断に引用不可。
- `review_criteria_20260908.md` 総論に「昇格判断はpromotion_governance.mdの
  必須条件に従う」を追記。実装・config変更なし（docsのみ）。

## R18-E: コストモデル定義+実測（ユーザー承認済み 06:27、タスク2/3）

- **現状確認（コードレベル）**: pnl_state.jsonのPnLに手数料/スリッページ控除なし
  （フィールド自体が不存在。経済性ゲート・反実仮想も同じ「コストゼロ世界」のPF）。
  全注文market注文。stale-fillガードで記録価格の一部はsizing_price（非fill）。
  entry_fill_id 0件/exit_fill_id 10件でfill台帳突合は不可 → 実測はpnl_state記録価格で実施。
- `scripts/analyze_execution_costs.py` 新規（読み取り専用）: 記録価格 vs 当日始値/
  前営業日終値のbps分布を時間帯バケット別に集計（714 leg、13 legレンジ外除外）。
  オープン直後entry median +49bps / 日中stop exit median +139bps はドリフト支配で
  「真のスリッページの上限推計」と明記。
- `docs/cost_model_definition.md` 新規: cost-adjusted PF = Σmax(pnl−cost,0)/|Σmin(...)|、
  cost=(entry+exit notional)×X/1e4、**保守的デフォルトX=5bps/片道**。
  試算: X=5で全期間PF 0.888→0.853（Net -$38k→-$51k）、直近コホート(n=45)
  0.530→0.511。経済性ゲートへは「補助表示→承認後Required化」の2段階案。
- `check_go_no_go.py` は未変更（スコープ厳守）。
  エビデンス: `docs/r18e_execution_cost_analysis_20260905/`。

## R19: 米国非ITユニバース shadow検証環境の構築（ユーザー承認済み 06:27、タスク3/3）

ユーザー決定⑥「取引は行わずshadowで検証環境の構築を進めたい」を受け、
R19として新規セクション起票+実装。**発注なし・本番無接触**。

- `scripts/log_us_nonit_universe_shadow.py` 新規（log_sector_rotation_shadow.py
  と同型）。本番実クラス（PriceMomentumFeature/BreakoutMomentumStrategy）を
  そのまま使用し、本番運用パラメータ（bar_limit=20/min_momentum=0.05/
  min_signal_strength=0.60、転記元をdocstringに明記）で非ITセクターETF 9本+SPYの
  would_signal等を `data/us_nonit_universe_shadow_log.jsonl` に日次記録。
  macro_regime係数はshadowでは無効（FRED配線なし、保守側）。
- テスト7件（`tests/unit/test_log_us_nonit_universe_shadow.py`）全緑。
- 手動実行1回 + cron強制実行1回で計2行のログ生成を確認。初回観測
  （2026-09-05）: would_signal 0/10（最接近はXLE momentum +6.45%だが
  ref_strength 0.32 < 0.60）。
- cron登録: `stock_swing_us_nonit_universe_shadow`
  （id `92748ee7-1e5b-4241-9968-ac117a9dd26e`、平日16:30 ET、command
  ペイロード、成功時NO_REPLY無音・失敗時のみTelegram通知）。強制実行で
  status ok / delivery silent（無音成功）を確認。
- docs: console_improvement_tasks.md に R19 セクション新設（目的/ユニバース/
  判定条件の出所/観測期間目安30営業日/昇格はユーザー判断・取引は行わない旨）。

## R20起票: 下落局面の武器(ロングオンリー制約下)

ユーザー確認により取引制限は「ショートのみ禁止」と確定(インバースETFのロング買付は可)。
R20として5候補を起票: A ディフェンシブ・ローテーション(R13-Dユニバース拡張、本命)/
B ボラターゲティング(R18-C統合)/ C 現金化階層 / D インバースETF戦術ロング(非レバ優先・
裁量発動禁止)/ E プロテクティブ・プット(中期)。すべてpromotion_governance.mdの
必須3条件に従う。実装・設定変更なし(起票のみ)。
