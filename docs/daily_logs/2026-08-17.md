# 2026-08-17

## ヘルスチェック（対応不要問題の確認）

コンソール `latest_console_summary.json` → health: status=OK, critical=0,
warning=0, guardrail_status=ok, ledger_gate_status=VALID, attribution=98.7%,
equity_bridge diff=$12,233（tolerance $100,000内）。緊急対応事項なし。

ロードマップ確認: 本日期限（🔲）の単発タスクなし。08-17〜08-28期間は
R4-v2/R5-v2の残タスク着手期間。R5-v2残は閾値のpaper観測待ち（09-04まで）
のため、着手可能なR4-v2の残項目に着手。

## R4-v2 残項目: raw score / normalized score 保存（着手・部分完了）

**対象**: `console_improvement_tasks.md` R4-v2セクション「未実装/未検証」の
1番目「raw score / normalized score / cross-sectional percentile 保存」

**実施内容**:
- `breakout_momentum_strategy.py`: `_calculate_raw_signal_score()` を新規追加
  （clamp・macro regime補正**前**の素の線形スコア、momentum/0.20、1.0超もそのまま保持）
- `event_swing_strategy.py`: 同様に `_calculate_raw_signal_score()` 追加
  （momentum * 5.0、event-timing/regime補正前）
- 両strategyの `CandidateSignal.metadata` に `raw_signal_score` /
  `normalized_signal_score`（＝既存の`signal_strength`と同値）を追加保存
- 既存の `signal_strength` 計算・フィルタリング挙動（min_signal_strength判定等）は
  一切変更なし。純粋な追加保存のみ
- `paper_demo.py` の R11-E `FeatureSnapshotStore` 配線（既存、08-15実装）が
  `signal.metadata` をそのままsnapshotに含めているため、追加の配線作業なしで
  `data/feature_snapshots/` にも自動的に記録されるようになった

**テスト**: `tests/unit/test_strategies.py` に4件追加
（raw/normalized両方がmetadataに入ること・raw scoreが1.0を超えてclampされない
ことを両戦略で確認）。フルスイート実行: **1858 passed**, 2 skipped（既存の
無関係スキップのみ、regressionなし）

## R4-v2 残項目（2）: cross-sectional percentile 保存（完了）

**実施内容**:
- `signal_prioritization.py` に `annotate_cross_sectional_percentile()` を新規追加。
  同一run内の全buy候補（breakout_momentum + event_swing）中で、各信号の
  signal_strengthが他候補に対して何%以上かを計算（最弱=0.0、最強=1.0、単一候補=1.0）
- `paper_demo.py`の`entry_signals = breakout_signals + event_signals`直後、
  `prioritize_buy_signals_v2()`呼び出し前に配線（metadataをin-placeで更新）
- `prioritize_buy_signals_v2`自体はsignal_strength/confidenceを直接参照して優先順位付けするため、
  今回のmetadata追加はソート順・セクターcap挙動に一切影響しない（回帰テストで確認済み）
- 既存の`FeatureSnapshotStore`配線経由で`data/feature_snapshots/`にも自動反映

**テスト**: `test_signal_prioritization_v2.py`に6件追加（ランキング正確性・非buy信号除外・
単一候補・空リスト・優先順位不変を確認）。フルスイート: **1863 passed**（2 skipped）。
`--dry-run`実run（43候補生成を確認）もエラーなく完了。

## R4-v2 残項目（3）: confidence calibration — 意図的に未着手（readiness gateのみ実装）

**判断**: confidenceはbreakout_momentum/event_swingのfixed multiplier（0.85/0.9）を
カリブレーション可能なprobabilityへ定義直す作業だが、実際のsizing（`confidence_multiplier`経由）
へ直接影響するため、十分なデータ蓄積（08-14のギャップ分析で推奨された目安100件）がないまま
実施するのは未検証な本番行動変更になりうる。実際に`data/decisions/`を集計したところ、08-14の
記録開始以降でも記録件数はわずか21件（目安の約1/5）のみであることを確認。

**よって今日はconfidenceの定義変更は実施せず、代わりに蓄積状況を自動可視化するreadiness gateを実装**:**
- `scripts/check_confidence_calibration_readiness.py` を新規作成（既存の
  `check_r8v2_ml_readiness.py`と同型パターン）。`data/decisions/*.json`の
  `evidence.sizing.confidence_multiplier`が記録されているdecision件数を集計し、
  100件の目安に対するready/not-readyを判定。`--save`で
  `reports/confidence_calibration_readiness.json`に保存。未達成時は非0終了
  （R8-v2同様の規約）
- 実行結果: `decisions_since_recording_started=31`, `with_confidence_multiplier=21`,
  `calibration_ready=False`（confidence_multiplier分布: 1.2×13, 0.7×4, 1.0×4）
- 既存08-25の`stock_swing_r4v2_progress_check_20260825`cronがこのresultsを自動検知できる

**テスト**: `test_confidence_calibration_readiness.py`に10件追加（08-14前のdecision除外・
未記録decisionの除外・集計正確性など）。フルスイート: **1873 passed**（2 skipped）

**未着手のまま残る項目**:
- confidence の実際の定義変更・calibration分析自体 — データが100件目安に達するまで意図的に保留
- decile別 expectancy / calibration curve 生成 — 未着手（PF/WRは実装済み）

## H9/R6-v2 残項目: config hash キャッシュ（完了）

**背景**: `MtimeFileCache`はmtime以前に`pnl_state.json`の全読込み問題を解決していた（08-27）が、
`symbol_registry.yaml`（小さな設定ファイル）はmodule-levelに`_yaml_cache`変数が定義されていた
だけで実際には使われておらず、ダッシュボードの3箇所（`_get_asset_class_for_symbol` /
`_get_buy_stop_list` / `_get_small_sample_watchlist`）が毎回`yaml.safe_load(read_text())`を
直接呼んでいた。特に`_get_asset_class_for_symbol`はopen position一件ごとに呼ばれるため、
`/api/dashboard`リクエスト1回でopen position数分同じYAMLを繰り返しパースしていた。

**実施内容**:
- `DashboardService._load_yaml_cached()` を新規追加（既存`_load_json_cached()`と同型の
  `MtimeFileCache`パターン、mtime/size変更時のみ再パース）
- 上記3箇所をすべてこのキャッシュ経由に変更

**テスト**: `test_dashboard_service.py`に`TestYamlConfigCache`を新規追加（5件）。キャッシュヒット、
未知シンボルフォールバック、registry不在フォールバック、ファイル更新後のキャッシュ失効を確認。
フルスイート: **1878 passed**（2 skipped）。実データ（symbol_registry.yaml）でもエラーなく
動作を確認済み。console自動再起動も正常完了。

**Learning制約**: 今回の変更はread-only observability追加のみ。フィルタ・
sizing・exit判定への自動接続は一切なし（既存R4-v2の制約を踏襲）。

## R7-v2 ロードマップ記載修正（ドキュメントのみ、コード変更なし）

**発見**: `console_improvement_tasks.md`のR7-v2セクションに「未実装」として残っていた2項目
（sourceごとのSLA/quality report、canonical schemaフィールド追加）を実コードで確認したところ、
**両方とも2026-08-14に実装済みだった**ことが判明（WebSocketの記載漏れと同種のドキュメント遅延）:
- source SLA: `system_adapter.py`の`_check_source_sla()`が4項目全て実装済み
  （broker≤30秒 / intraday quote≤2分 / daily bar / sector benchmark）、テストも既存
- canonical schema: `RawEnvelope`データクラスに該当フィールド全て定義済み、全7ソースの
  `_write_raw_snapshot()`経由で実データ（`data/raw/broker/*.json`等）に反映確認済み

**対応**: `console_improvement_tasks.md`の該当箇所をVERIFIED_COMPLETEに訂正。コード変更は
なし（既に実装済みのため）。これでR7-v2の本当の残項目はもはやWebSocketのみ（09月以降着手予定）。

## R4-v2 残項目(4): decile別 expectancy 追加（完了、calibration curveは未着手のまま）

`scripts/analyze_signal_strength_decile.py`の`compute_decile_stats()`に`expectancy`
（decile内の1トレード平均PnL）を追加。net_pnl（合計値）だけでは件数の異なるdecile間の
比較が歪むため、正規化した指標として導入。テーブル出力・JSON保存両方に反映。

実行結果（08-17時点、実データ）: decile 5が最良（+$832/trade, PF=2.455）、
decile 8が最悪（-$1,670/trade, PF=0.116）。既存のPF/WRベースの結論（decile 5が
最良）と整合。

テスト3件追加（`test_analyze_signal_strength_decile.py`、全16件pass）。フルスイート:
**1881 passed**（2 skipped）。既存consoleコンポーネントはこのJSONを参照していないため
表示への影響なし（純粋な追加フィールド）。

calibration curve生成（confidence/signal_strengthとexpectancyの連続的な関係を
モデル化する部分）は引き続き未着手。
