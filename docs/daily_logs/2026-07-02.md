# 2026-07-02

## システム状態

- Cron: 12本 全 `lastRunStatus=ok`
- Tests: 664 passed, 2 skipped
- Equity: $1,077,300（07-01 スナップショット）
- Attribution: 100%（post-R1-B）

---

## 本日対応タスク

### ✅ `docs/console_improvement_tasks.md` 更新

07-01 の大規模実装成果をタスクファイルに反映。

**変更内容**:
- スケジュール概要（07-07〜07-09 / 07-07〜07-14 / 07-14〜07-18）→ ✅ 前倒し完了に更新
- 08-05〜08-18 セクション: Guardrail hard-halt → 完了済みに修正
- R0 残タスク: チェックボックス2件を `[x]` に更新
- R3-A: `🔲` → `✅ 2026-07-01`
- R4-A: `🔲` → `✅ 2026-07-01`
- R6-D: `🔲` → `✅ 2026-07-01`
- R3-B 目標日: 07-10〜07-14 → 07-09〜07-14（R3-A 完了を受けて）
- 次のアクション（週次）: 前倒し完了分を完了済みに移動・週次ブロックを再構成
  - R2-D / R3-A / R4-A / R6-D / Guardrail hard-halt / ランブック2件 → 完了済みへ
  - Week 1-2: R3-B のみ残り
  - Week 3: R6-F
  - Week 4: Go/No-Go チェック・最終判定
- 優先順位まとめ: R0/R3/R4/R6 の状態を最新に更新

---

## 改善計画の現在位置

| フェーズ | 状態 |
|---|---|
| R0 | ✅ 完了（hard-halt 有効化済み · 30日検証中〜07-31） |
| R1 | ✅ 完了（attribution 100%） |
| R2 | ✅ **全完了**（R2-A/B/C/D） |
| R3-A | ✅ 完了（07-01） |
| R4-A | ✅ 完了（07-01） |
| R6 C1/C2/D | ✅ 完了 |
| R3-B | 🔲 07-09〜07-14 |
| R6-F | ✅ 完了（07-02 前倒し） |
| Go/No-Go 最終判定 | 🔲 07-31 |

---

---

## ✅ R3-B: Exit Replay 評価（本日前倒し完了）

`scripts/compare_exit_variants.py` で 227 closed trades を 5バリアントで bar-by-bar リプレイ。

| Variant | Total PnL | Δ vs Baseline | PF |
|---|---|---|---|
| A: baseline（現行） | $31,876 | — | 1.21 |
| B: +break_even | $27,581 | **-$4,295** | 1.18 |
| C: +stalled_winner（積極） | -$37,333 | **-$69,209** | 0.77 |
| C6: +stalled_winner（保守） | $30,305 | -$1,571 | 1.20 |
| **D: +staged_trailing（推奨）** | **$36,965** | **+$5,089** | **1.24** |

**結論**: Variant D（staged trailing）が最良（+16%改善）。Break-even stop は廃止検討。主課題は exit 戦略ではなく実行ラグ（discrete paper_demo）。  
詳細: `docs/r3b_exit_replay_report.md`

**要注意 open ポジション**: CHPX(-11.3%), MU(-7.3%), NBIS(-6.9%), FTXL(-6.1%)  
→ 本日の paper_demo で stop_loss / trailing_stop が発動する可能性あり。

---

## ✅ R4-B Option A: Signal Strength 飽和修復（本日前倒し完了）

- `breakout_momentum_strategy.py`: `min(momentum / 0.10, 1.0)` → `min(momentum / 0.20, 1.0)`
- `min_signal_strength` デフォルト: `0.65` → `0.40`（実効最低 momentum 閾値を維持）
- `paper_demo.py` CLI デフォルト: `0.65` → `0.40`
- `safe_ranges.py` min_value: `0.4` → `0.3`
- 期待効果: strength=1.0 飽和率 73% → 約 30%
- commit: `588f643`

---

## ✅ guardrail_calibration.md 作成（R0 残タスク完了）

`docs/runbooks/guardrail_calibration.md` — 閾値9項目の根拠・キャリブレーション実績を記録。
- 最悪日（06-08: -4.35%）が -3.5% halt 閾値の根拠であることを確認
- warning_only 6日間ゼロ誤発動の実績を記録

---

## 改善計画の現在位置（07-02 更新）

| フェーズ | 状態 |
|---|---|
| R0 | ✅ 完了（hard-halt + キャリブレーション runbook） |
| R1 | ✅ 完了 |
| R2 | ✅ 全完了 |
| R3 | ✅ **全完了**（R3-A + R3-B） |
| R4-A/B | ✅ 完了（07-01/07-02） |
| R4-C | 🔲 07-28〜08-04 |
| R6 C1/C2/D/E/F | ✅ 完了 |
| Go/No-Go 最終判定 | 🔲 07-31 |

---

## 次のアクション（優先順）

1. **Go/No-Go 最終判定**: 07-31
2. **R4-C**: signal strength デサイル検証（07-28〜08-04）

---

## ✅ Variant D staged trailing 実装（本日前倒し完了）

R3-B の推奨結果を受けて、`SimpleExitV2Strategy` に staged trailing を feature flag 付きで実装。

**実装内容**:
- `simple_exit_v2_strategy.py`: `staged_trailing_enabled` / `staged_trailing_levels` を追加
- `config/strategy/simple_exit_v2.yaml`: staged trailing を有効化
  - +5% 到達: 3.5% pullback
  - +8% 到達: 3.0% pullback
  - +12% 到達: 2.5% pullback
- `paper_demo.py`: YAML 設定を `SimpleExitV2Strategy` に渡す
- テスト追加: staged trailing が baseline より早く発火するケース

**ロールバック**:
`config/strategy/simple_exit_v2.yaml` の `staged_trailing_enabled: false` で即時無効化可能。

---

## ✅ R6-E Attribution パネル（本日前倒し完了）

exit_reason 別の PF / WR / net PnL を console に表示するパネルを追加。

**実装内容**:
- `PnLTracker.get_exit_attribution_breakdown()` 追加
- `ConsoleSummary`: `exit_attribution_breakdown` フィールド追加
- `ConsoleRenderer`: `EXIT ATTRIBUTION` セクション追加
- `paper_demo.py`: console summary に attribution breakdown を渡す
- テスト追加: exit_reason 別集計・console 表示

---

## ✅ Go/No-Go チェックリスト定義（本日前倒し完了）

`docs/runbooks/go_no_go_checklist.md` を新規作成。

**内容**:
- Critical Gates
- Performance Gates
- Safety Gates
- Implementation State
- 07-31 最終 Decision Record

`docs/runbooks/live_mode_switch.md` の R3/R4 状態も最新化。

---

## 検証

- Targeted tests: 53 passed
- Full tests: **667 passed, 2 skipped, 1 warning**

---

## ✅ R6-F Remote Web 読み取り専用監視（本日前倒し完了）

スマートフォン対応の read-only remote monitor を、既存の操作系 console とは分離して実装。

**実装内容**:
- `console/remote_readonly_app.py` 新規作成
  - `GET /health`
  - `GET /api/status`
  - `GET /api/console_summary`
  - `GET /api/go_no_go`
  - `POST` / `PUT` / `DELETE` は 405 read_only
- `console/ui/mobile_readonly.html` 新規作成
  - Run Health
  - Portfolio
  - Exit Attribution
  - Broker / Tracker
  - Decision Funnel
  - Alerts
  - Go / No-Go
- token 認証: `REMOTE_READONLY_TOKEN`
- デフォルト bind: `127.0.0.1:3340`
- runbook: `docs/runbooks/remote_readonly_monitor.md`

**セキュリティ方針**:
- 既存 `console/app.py` は parameter apply/rollback など操作系 API を持つため、遠隔公開しない
- R6-F server は生成済み JSON / markdown の読み取りのみ
- 外部公開する場合は VPN / OpenClaw gateway 等の追加アクセス制御を推奨

**検証**:
- Targeted tests: 5 passed
- Full tests: **672 passed, 2 skipped, 1 warning**

---

## ✅ R6-F 公開方式方針決定 + LAN確認

**方針**:
- 今日の確認: LAN / local
- 08-01 までの実運用候補: OpenClaw gateway

**LAN確認結果**:
- Mac LAN IP: `192.168.0.190`
- 起動: `REMOTE_READONLY_HOST=0.0.0.0`, `REMOTE_READONLY_PORT=3340`
- local `/health`: 200
- LAN IP `/health`: 200
- LAN IP authenticated `/api/status`: 200
- LAN IP unauthenticated `/api/status`: 401

**次の課題**:
- OpenClaw gateway で read-only monitor へのルート設計
- raw port forwarding は使用しない

### 追加対応: LAN確認サーバーを launchctl 常駐化

スマホから見えない原因を確認したところ、一時セッション起動の read-only server が終了していた。

**対応**:
- `~/Library/LaunchAgents/com.hirotomookawasaki.stock_swing.remote_readonly.plist` を作成
- `REMOTE_READONLY_HOST=0.0.0.0`
- `REMOTE_READONLY_PORT=3340`
- `KeepAlive=true`

**再確認**:
- listen: `*:3340`
- local `/health`: 200
- LAN IP `/health`: 200
- LAN IP authenticated `/api/status`: 200
- LAN IP unauthenticated `/api/status`: 401

### 追加対応: スマホUIのトークン入力改善

スマホからページは見えるが各項目が空欄になる事象を確認。Mac側 API は数値を返していたため、ブラウザ側で token が保存されていないケースが主原因と判断。

**対応**:
- `console/ui/mobile_readonly.html` が `?token=<REMOTE_READONLY_TOKEN>` を読み取り localStorage に保存
- 保存後は address bar から token query を除去
- API 取得失敗時は空欄ではなく `Data unavailable: ...` を各パネルに表示

### 追加対応: スマホUIの情報量拡張

スマホ側で最低限見たい項目として `Open Positions` / `Recent Trades` を追加。あわせて運用監視に必要な 3 パネルも追加。

**追加パネル**:
- Open Positions: symbol / qty / entry / current / hold / unrealized または return
- Recent Trades: 直近クローズ取引、exit_reason、strategy、PnL
- At-risk Positions: -5% 以下、利益剥落、current 不明の高ピーク銘柄
- Cron / Guardrail Health: cron job 状態、guardrail、circuit breaker
- Broker/Tracker diff detail: broker_only / tracker_only / qty_mismatches

**追加 API**:
- `GET /api/positions`
- `GET /api/recent_trades?limit=25`
- `GET /api/at_risk_positions`
- `GET /api/operational_health`
- `GET /api/broker_tracker_detail`

**検証**:
- Targeted tests: 9 passed
- Full tests: **676 passed, 2 skipped, 1 warning**
