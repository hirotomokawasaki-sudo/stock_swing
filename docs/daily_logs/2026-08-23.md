# 2026-08-23（日）

## システム健全性チェック（10:09 JST）

- console health: OK（critical=0, warning=0）
- guardrail: ok / ledger_gate: VALID / attribution: 98.8%
- equity bridge diff: $12,398（許容$100,000以内）
- 全25 cronジョブ: 直近実行すべて `ok`
- 本日は「次のアクション」ロードマップ上、日付指定タスクなし
  （08-24〜09-04 R5-v2 promotion gate観測期間の前日。cronは既に稼働中）

## R4-v2残項目: calibration curve生成（完了）

**背景**: `docs/console_improvement_tasks.md` のR4-v2セクションで「decile 別
expectancy / calibration curve生成」のうちexpectancy部分は08-17完了済みだったが、
calibration curve部分は未着手のまま残っていた。

**実装**:
- `scripts/analyze_signal_strength_decile.py` に `compute_calibration_curve()` /
  `print_calibration_curve()` を追加
  - 各decileの signal_strength midpoint（ss_min/ss_maxの中点）を「予測勝率」の
    プロキシとみなし、実際の win_rate と比較する read-only診断
  - 出力: `reports/signal_strength_decile.json` に `calibration_curve` フィールド
    として追加（既存フィールドは無変更）
  - Learning制約に準拠: recommendation-only、自動閾値変更なし
- テスト6件追加（`test_analyze_signal_strength_decile.py`、全22件PASS）

**実行結果（2026-08-23、実データ n=110）**:
- Mean calibration error: **38.8%**（大きい）
- decile 8〜10（signal_strength=1.000で飽和）の predicted=100% に対し actual WR は
  36〜46%と大きく乖離 → **signal_strengthは現状「勝率の確率」としては全く較正されて
  いない**ことを定量的に確認
- 既存のR4-v2の知見（confidence固定0.85多発、confidence calibration未着手の判断）と
  整合する結果。confidence/signal_strengthを確率として直接使う設計変更は
  依然として時期尚早と判断

**次のアクション**: ロードマップのR4-v2セクションに完了追記予定。他は変更なし。

## R7-v2 canonical schema拡張の確認

- 昨日までのヒアリングで「未着手」候補として挙げていたが、実データ確認の結果
  `data/raw/finnhub/*.json` に event_time/available_at/ingested_at/source_id/
  revision_id/quality_status が全て記録済みと確認 → ドキュメント記載通り
  **既にVERIFIED_COMPLETE**（誤って未着手と判断していたのを訂正、実装変更なし）

## Go/No-Go判定基盤・現状パフォーマンスの実データ監査（ユーザー依頼、11:02〜13:00 JST）

**依頼内容**: 「現在の投資システムの現状のパフォーマンスと改善計画の監査、
パフォーマンス向上のための提案」→ 実データ精査後、監査結果を踏まえて
「改善計画の修正や修正用のコード作成（実装は別途）」の追加依頼。

**パフォーマンス監査結果（実データ）**:
- 口座評価額 $971,326.96（元本比-2.87%）、確定PnL -$20,573.51、含み損 -$27,038.28
- 全252件 PF=0.913 WR=47.6%、attributable 49件のみ PF=1.082（95%信頼区間
  約0.50〜2.43、正のエッジは統計的に未確定）
- exit別: trailing_stop PF=7.64（機能）、stop_loss PF=0.109（-$151,696、
  8/5以降11件全損失）
- 暫定Sharpe -0.75、SPY比アルファ約-11.3pt、実現損益カーブ最大DD約9.28%

**Go/No-Go判定基盤の実バグ発見（6件、詳細は`docs/console_improvement_tasks.md`
「2026-08-23 監査」節参照）**:
1. 🔴 `paper_demo.py`: 無アクションrun（no_signals/no actionable decisions）が
   早期returnし`console_summary.emit()`を一切実行しない。実際に**08-22/08-23の
   2日間console summaryが凍結**していたことを確認（decisionファイル0件）
2. 🔴 equity_bridgeの`quarantined_pnl=0.0`固定値バグ。quarantined_trades 101件
   全件が実際のbroker約定（PnL約-$156,476）だったにもかかわらず0円扱いにされ、
   本来$168,874ある`unexplained_diff`が$100,000 toleranceでマスクされていた
3. 🟡 `collect_broker_bars()`のページネーション未処理。`fetch_bars(limit=5)`が
   `start`未指定のため常に最古の約5営業日を取得し続け、pairwise correlationの
   入力データが**何ヶ月も同じ2026-07-23〜07-29の凍結データ**だった
4. 🟡 `check_go_no_go.py`のRequired条件3件（cron_jobs_healthy /
   paper_3day_confirmation、及び存在しなかったconsole鮮度チェック）が実質
   恒久PASS化する設計だった（固定ファイルgrep、health.status再読のみ等）
5. 🟢 `f8_clean_records_analysis.py`のexpectancy計算式バグ（-$10,772.71 vs
   正しい値-$81.64、2桁乖離）
6. 🟢 同スクリプトのmax_drawdown分母誤り（累積PnLピーク基準→account equity
   基準に修正、11.58%→9.28%）

**対応**: 修正コード6件を作成し実データ・既存テストで動作確認済み
（フルスイート2065 passed / 2 skipped、regressionなし）。**ユーザー指示に
より本番適用は別途実施**するため、`docs/audit_fixes_20260823/`に
git diff形式のパッチとREADMEとして保存し、作業ツリー自体は元の状態に
差し戻した（6ファイルとも`git checkout`で復元済み）。
`docs/console_improvement_tasks.md`に監査結果セクションを新規追加し、
Pre-Launch Gate Review（09-08〜09-12）にパッチ適用確認タスクを追記。

## 監査修正パッチ6件を本番適用（12:45 JST、ユーザー承認）

上記監査で1つにまとめて作成した6パッチを`git apply`で01→6の順番で適用
（03→4の依存を守り、全件`--check`済み）。

**適用後の検証結果**:
- フルテストスイート: **2065 passed / 2 skipped**（regressionなし）
- `--dry-run`実行（発注なし、broker.fetch_account/fetch_positions/fetch_barsの
  参照系APIのみ）: equity_bridgeが
  `unexplained_diff=+168,869.89`、`within_tolerance=false`を正しく表示することを
  確認（修正前は$12,393でwithin_tolerance=trueと誤表示されていた）
- `python scripts/check_go_no_go.py`実行: 新規`console_summary_freshness`条件が
  意図通り❌（46.91h old）を検知しネ埋吹バグを可視化。`cron_jobs_healthy`は
  実際のcron実行履歴（25/25 jobs parsed）、`paper_3day_confirmation`は直近7日間
  の実日付確認（6日）に変更されていることを確認
- `scripts/f8_clean_records_analysis.py --json`実行: `expected_value == avg_pnl ==
  -81.64`一致、`max_drawdown=9.28%`（baseline $1,000,000）を確認
- `pairwise_correlation.check_data_freshness()`単体実行: 既存データ（2026-07-29最新）
  に対し正しく`fresh=False`を返すことを確認（次回`stock_swing_news_collection`
  cron以降、修正済み`collect_broker_bars()`が新規日付のbarを取得し始める見込み）

**ロードマップ更新**: `docs/console_improvement_tasks.md`の監査セクション全件を
Status PATCH_READY→APPLIEDに更新し、適用日時と検証結果を追記。残っている
運用判断（equity_bridge $168,869.89差分の处理方釕）はPre-Launch Gate Review
（09-08〜09-12）までに実施することをスケジュールに明記。

**本番コードへの変更ファイル**: `src/stock_swing/cli/paper_demo.py`、
`src/stock_swing/cli/collect_data.py`、`src/stock_swing/risk/pairwise_correlation.py`、
`scripts/check_go_no_go.py`、`scripts/f8_clean_records_analysis.py`、
`tests/unit/test_check_go_no_go.py`（内容は`docs/audit_fixes_20260823/README.md`参照）。
発注ロジック・ポジションサイジング・ガードレール閾値は一切未変更。

**次のアクション**: 本日以降のcron実行（premarket/market_open/midday/
market_close等）が修正後のコードパスで正常稼働するかを引き続き監視。
`quarantined_pnl`・toleranceの運用判断をPre-Launch Gate Reviewまでに行うこと。

## 投資戦略レビュー + Claude独立検証パケット作成 (13:19〜14:10 JST)

**依頼内容**: 「投資戦略の改善計画・改善内容自体をレビューして改善点を提案」
→「Claudeで検証するための検証材料・ソースを準備」→「検証パケットを確認し、問題なければ
改善検討」→「すぐに実装すべきものに対応」と依頼が連鎖。

**戦略レビュー主張（14件）**: attributable 49件のedgeはbootstrap 90%区間
0.564】2.125で統計的に未確定、R11バックテストは同日closeをsignal入力とentry価格の
両方に使うlook-aheadバイアス、survivorship biasあり、"breakout"の実装は単純な
20日リターンしか見ていない、high-confidence sizing 1.2倍はcapでclipされ実質的
にno-op、promotion PFの母集団がattributableではなく全closed 252件、top5
集中度の分母がgross exposure基準なのに40%閾値はequity基準、dry-runが本番と同じ
 console summaryを上書きできる、など。

**検証パケット作成**: `docs/strategy_review_claude_validation_20260823/`を新規作成。
匿名化した252件のclosed tradeスナップショット、bootstrap再計算スクリプト、
14件の主張をそれぞれ支持/反証条件付きで整理したCLAIMS_MATRIX.md、実装価値評価案、
外部一次資料（FINRA/SEC/学術論文、賣否両論含む）、SHA-256マニフェストを同梱。
パッチや設定変更は一切含まず、発注なし。

**パケットの自己検証**（ユーザー依頼「Codexレビューが正しいか検証して問題なければ
改善検討」）: パケットのスクリプト実行だけでなく、source_files/をライブリポジトリと
`diff`（完全一致確認）、主張8件（C02/C03/C05/C08/C09/C10/C11/C12）を実コードで
直接再検証してすべて確認。**この検証中に、今朝cf6bc75の修正自体に残っていた
ギャップ（C12）を発見**：`SystemAdapter._fetch_one_job_runs()`がJSON解析成功のみを
見ており、実際の`lastRunStatus`フィールドを一切評価していなかった。

**結論**: Codexレビューの事実認定8件は全てライブコードで再現。外部引用も賣否両論を
バランスよく含む。09-15の実資金投入は現時点のエビデンスでは正当化できないと判断。

## レビュー対応: 「すぐに実装すべき」で6件を実装・適用（13:38〜14:15 JST）

上記レビューのIMPLEMENTATION_VALUE.mdで`IMPLEMENT_NOW`判定だった低リスク項目（発注
ロジック未変更、状態可視化のみ）を6件実装・適用（ユーザー承認）。commit `f204336`。

1. **dry-run証跡の分離**: `ConsoleSummary`に`dry_run`/`invocation_source`フィールド
   を追加。`check_go_no_go.py`に新規`console_summary_not_dry_run`条件追加。
   実験: 今回の診断用`--dry-run`実行直後に`check_go_no_go.py`を実行したところ、
   意図通り❤（dry_run=True）を検知しNO-GOを維持（修正前はGOに変わってしまう問題を
   実際に確認済み）。
2. **cron健全性の実 status評価**（検証中発見のバグ修正）: `_check_cron_run_history()`に
   `lastRunStatus`/`consecutiveErrors`実評価を追加。
3. **paper 3日gate強化**: `daily_report_morning`が書く汎用bookkeeping snapshot
   （decisions_generated=0）と、実際のpaper_demo scheduled runを区別。実データで
   08-21/08-22の2日間がdecisions_generated=0のshallow snapshotだったことを確認。
4. **promotion PFの母集団をattributable限定**: `PnlTracker.get_attribution_quality_
   breakdown()`と同じ分類基準を`check_go_no_go.py`に適用。attributable 49件のみで
   PF=1.082を計算（検証パケットの値と完全一致）。
5. **top5指標の分離**: `dashboard_service._summarize_positions()`に
   `top5_concentration_equity_pct`/`_gross_pct`/`gross_exposure_pct_of_equity`/
   `hhi`を追加。`promotion_gate.py`の40%閾値評価をequity基準に変更。実データで
   gross基準50.7% vs equity基準31.7%、gross_exposure/equity=62.5%という
   乗離を確認。
6. **未使用YAMLフラグにUNUSED注記**: `breakout_momentum_v1.yaml`/`event_swing_v1.yaml`
   自体がどこからも読み込まれていないことをgrepで確認し、既存のmax_hold_days UNUSED注記と
   同形式で明記。

**検証**: フルテストスイート2065 passed / 2 skipped。実`--dry-run`実行で新規4条件
（console_summary_not_dry_run/paper_3day_confirmation/clean_cohort_pf/
top5_concentration）が全て意図通り動作することを確認済み。発注ロジック・
ポジションサイジング・ガードレール判定は一切未変更。

**未実施（Paper A/B先行推奨）**: signal_strengthとexit/sizingの切り離し、
high-confidence sizing no-op修正は、挙動変更を伴うため未実施。IMPLEMENTATION_VALUE.md
でPAPER_AB_FIRST判定された項目、及びR11再構築などの長期研究項目は未着手。

**スキル化**: 今回の一連のワークフロー（実データ監査→エビデンス付き主張整理→
テスト済みパッチ→承認後適用→第三者検証パケット）を`skill_workshop`でスキル化。
ユーザー承認済み、プロポーザルID `evidence-based-system-audit-20260823-d6294d652d`
（pending状態、未適用）。

## quarantine 101件の内訳調査と根本原因修正（14:24〜15:45 JST）

**依頼内容**: equity_bridgeの$168,869.89未説明差分の処理方遵（(a)再統合 vs
(b)tolerance引上げ）のメリデメを説明した後、「みてみて」との依頼を受けquarantine 101件
の内訳を実データで調査。

**発見1（中間）**: 101件中70件（69%）が同一`exit_broker_order_id`を他のquarantine件と
共有しており、かつ買いが売りより後の日付というFIFO会計として不可能な状態を確認
（例: DELLの05-14 sell 1件が05-20〜07-01の5つのbuyとそれぞれ独立にマッチ）。

**根本原因の特定（実データで確認）**: `rebuild_pnl_state_from_broker.py`の
`fetch_all_filled_orders()`が`broker.fetch_orders(status='all', limit=500)`を
**単発呼び出し**しており、Alpaca APIはpaginationなしでは直近500件しか返さないことを
実口座で実証: 単発`limit=500`呼び出しは2026-05-21以降の注文しか返さないが、
`direction=asc`+`after`ページネーションで完全取得すると**2026-05-12から存在**することを
確認（約9日間・約200件の注文が黙って欠落）。FIFOマッチャーは欠落した古いbuyを
検知できず、手元にある次善（実際は売りより後の日付）のbuyを誤ってマッチさせていた。
101件中96件（migrate_quarantine_invalid_trades 54件 +
reversed_chronology系46件）がこの一つの原因で説明可能と判明（残も1件はklac_split_
anomaly、既知・別原因）。

**修正内容**（commit `adad1db`）:
- `src/stock_swing/sources/broker_client.py`: `fetch_all_orders()`を新規追加
  （`direction=asc`+`after`ページネーション、`max_pages`安全装置と`truncated`フラグ付き、
  既存`fetch_orders()`は後方互換のため無変更）
- `scripts/rebuild_pnl_state_from_broker.py`: `fetch_all_filled_orders()`を
  `fetch_all_orders()`使用に変更

**実データ検証（読み取り専用、pnl_state.json等の書き込みは一切なし）**: 実口座で
`fetch_all_orders()`実行→691件（2ページ、truncated=False）、2026-05-12〜08-20の
完全な履歴を取得できることを確認（修正前は500件・2026-05-21以降のみ）。

**テスト**: 新規8件追加（ページネーション、cursor進行、重複排除、max_pages上限、
status pass-through等）。フルテストスイート**2073 passed / 2 skipped**（regressionなし）。

**次のアクション（本修正の範囲外）**: この修正は今後のrebuild実行を正しくするだけで、
既存quarantine 101件自体の再統合は未実施。本番`pnl_state.json`の書き換えを伴うため、
実施は別途の承認プロセスを経ること。

## R13-B: signal_strength/exit接続 Option Bヒストリカル検証（追記）

R13-Bのうちexit側Option B（切り離し案）を、R13-Aと同じ日次パスリプレイ手法で検証。
`scripts/simulate_signal_strength_exit_decoupling.py`新規作成。結果: 全110件で
net -$1,804（弱い悪化）、attributable 41件でnet +$1,457（弱い改善）。ただし
両方とも変化トレード数が2〜9件と極めて少なく統計的根拠として不十分。R13-Aほど
極端な逆転ではないが同じ「少数トレード依存で頑健性が低い」という弱点。
paper A/Bへ進む根拠は現時点で不十分と判断、R13-C後に再検証する方針。sizing側
（confidence_multiplier no-opバグ修正）は固定qty履歴で直接バックテストできない
ため別途検証手法の設計が必要、未着手のまま。テスト2073 passed/2 skipped（変化なし）。

エビデンス: `docs/r13b_signal_strength_decoupling_validation_20260823/`

## R13-C: R11バックテスト基盤の根本再構築（最小構成、強い肯定的知見）

R13-Cの最小構成（項目1: t+1約定、項目2: point-in-time universe）を実装・検証。
`scripts/r11_backtest_engine_v2.py`（新規）と`scripts/r11_symbol_universe_intro_
dates.py`（新規、git履歴から各銘柄のuniverse登録日を逆算）を作成。

**検証結果（全69銘柄、2年分、実価格）**: 旧v1エンジン（look-ahead+survivorship
バイアス入り）はPF=1.733、修正後v2エンジンはPF=2.069。分解分析で、v1の全1,415件
取引中1,029件（73%）が実際の銘柄導入日以前に発生していたことを確認（例: NBISは
intro=2026-04-23だが2024年から取引され+$17,498計上）。

**外部検証（重要）**: 修正版backtestの月次PF推移（05月4.53→06月0.39→07月0.89→
08月2.81）が、実際の本番取引の月次PF推移（05月4.53→06月0.55→07月0.28→08月1.08）
と方向性が完全一致。エンジンがモデリングアーティファクトではなく実際のシグナル
レジーム依存性を再現できているという強いシグナル。

本番go-live以降（2026-05-12〜）のみでPF=1.448（ブートストラップ90%CI
[1.099, 1.891]、完全に1.0を上回る）。現在の本番attributable PF=1.082
（90%CI 0.564–2.125、中心が1.0をまたぐ）より強い。

**ただし過大評価しない**: exit側look-ahead未対応（項目3）、slippage未反映
（項目5）、exposure/sector cap未反映（項目4）など未実装の残り5項目により
楽観側に偏っている可能性が高い。breakout_momentumシグナル自体には一定のエッジが
ある可能性を示すデータだが、本番パラメータ変更の根拠にはまだしない。

テスト: `tests/unit/test_r11_backtest_engine_v2.py`新規5件（合成データでt+1 fill・
universe gateのメカニズムを検証）。フルスイート2078 passed/2 skipped（regression
なし）。

エビデンス: `docs/r13c_backtest_v2_validation_20260823/`

## R13-C 継続: 項目3（保守的OHLC exit）・項目5（slippage）実装（v3）→ 結果ほぼ不変

同日内に継続してR13-Cの残り2項目を実装。`scripts/r11_backtest_engine_v3.py`新規作成:
項目3（peak_priceを日次HIGHで更新し、stop/trailing/breakevenの下限判定を日次LOWで
チェック）と項目5（本番は全てmarket orderであることを確認した上でentry/exit両方に
one-way slippage_bpsを不利方向に適用）。

**検証結果（全69銘柄、2年分、片道slippage=10bp）**: v2の go-live以降PF=1.448に対し、
v3（保守的exit+slippage両方適用）はPF=1.453で**ほぼ不変**。往復30bpスリッパージという
既存分析で使っていた最も保守的なシナリオでもPF=1.426で大きく崩れない。ブートストラップ
90%CI=[1.210, 1.750]（v2の[1.099, 1.891]より幅が狭まり下限上昇、リサンプルの100%がPF>1）。
本番との月次PF方向性一致もv3で維持。

**重要な評価**: 当初の想定（保守的補正でPFはかなり削られるはず）に反し、実際にはほぼ
不変だった。breakout_momentumシグナル自体のエッジが楽観的なモデリング前提（クローズ仲値や
フリクションレス約定）に依存していなかったことを示す強いシグナルだが、残る項目4（exposure/
sector cap）・6（walk-forward+embargo）・7（trial registry）は依然未実装。

テスト: `tests/unit/test_r11_backtest_engine_v3.py`新規5件（保守的OHLC exitのLOWトリガーと
close-onlyモードとの挿分け、slippageのentry/exit両方への不利方向適用、trailing_stop優先順
保持を検証）。フルスイート2083 passed/2 skipped（regressionなし）。

エビデンス: `docs/r13c_backtest_v3_validation_20260823/`

次のアクション: 項目4（exposure/sector cap）はentry filter層との統合が必要で後回し。

## 変更ファイル

- `scripts/analyze_signal_strength_decile.py`（+58行）
- `tests/unit/test_analyze_signal_strength_decile.py`（+44行）
- `reports/signal_strength_decile.json`（再生成、calibration_curveフィールド追加）
- `docs/console_improvement_tasks.md`（監査結果セクション追加、Pre-Launch Gate
  Reviewスケジュールにパッチ適用確認タスク追記）
- `docs/audit_fixes_20260823/README.md`（新規、監査詳細）
- `docs/audit_fixes_20260823/patches/*.patch`（新規6件、適用済みのgit diff）
- `docs/strategy_review_claude_validation_20260823/`（新規、Claude独立検証パケット）
- `config/strategy/breakout_momentum_v1.yaml` / `event_swing_v1.yaml`（UNUSED注記追加）
- `console/adapters/system_adapter.py`（cron実status評価）
- `console/services/dashboard_service.py`（top5指標分離）
- `scripts/check_go_no_go.py`（dry-run除外、paper3日強化、attributable限定）
- `src/stock_swing/cli/paper_demo.py`（dry_run/invocation_source配線）
- `src/stock_swing/reporting/console_summary.py`（dry_runフィールド追加）
- `src/stock_swing/risk/promotion_gate.py`（top5 equity基準評価）
- `tests/unit/test_check_go_no_go.py` / `test_promotion_gate.py`（テスト更新）
- `src/stock_swing/sources/broker_client.py`（`fetch_all_orders()`新規追加）
- `scripts/rebuild_pnl_state_from_broker.py`（ページネーション対応）
- `tests/unit/test_broker_client_fetch_all_orders.py`（新規8テスト）
- `scripts/simulate_signal_strength_exit_decoupling.py`（新規、R13-B exit側検証）
- `docs/r13b_signal_strength_decoupling_validation_20260823/`（新規、エビデンス）
- `scripts/r11_backtest_engine_v2.py`（新規、R13-C最小構成）
- `scripts/r11_symbol_universe_intro_dates.py`（新規、point-in-time universe逆算）
- `tests/unit/test_r11_backtest_engine_v2.py`（新規5テスト）
- `docs/r13c_backtest_v2_validation_20260823/`（新規、エビデンス）
- `data/r11_price_cache/_symbol_universe_intro_dates.json`（新規、銘柄導入日付キャッシュ）
- `scripts/r11_backtest_engine_v3.py`（新規、R13-C項目3・5）
- `tests/unit/test_r11_backtest_engine_v3.py`（新規5テスト）
- `docs/r13c_backtest_v3_validation_20260823/`（新規、エビデンス）
- 本ファイル更新
