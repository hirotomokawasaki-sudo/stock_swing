# 2026-06-17

## Summary

- Implemented a low-risk cron token reduction pass for `stock_swing` without lowering
  trading/check frequency.
- Added machine-readable one-line cron summaries and changed cron wrappers so verbose
  execution output stays in log files while stdout returns only the final compact
  `CRON_SUMMARY_JSON=...` line.
- Confirmed wrapper behavior locally with `run_paper_demo.sh --dry-run`, which emitted
  the expected compact summary line only.

## Cron Token Reduction Changes

- Added `src/stock_swing/cli/cron_summary.py` with a shared compact JSON emitter for
  cron-facing commands.
- Updated these CLIs to support compact cron summaries:
  - `src/stock_swing/cli/collect_data.py`
  - `src/stock_swing/cli/reconcile_orders.py`
  - `src/stock_swing/cli/paper_demo.py`
- `collect_data.py` also gained a best-effort runtime ceiling for Finnhub collection via
  `--max-runtime-seconds` / `COLLECT_DATA_MAX_RUNTIME_SECONDS` to reduce token spikes
  during long or stuck cron runs.
- Updated these wrappers to keep cron stdout minimal and preserve detailed logs on disk:
  - `scripts/cron/collect_data.sh`
  - `scripts/cron/reconcile_orders.sh`
  - `scripts/cron/run_paper_demo.sh`

## Validation

- Syntax / structure checks passed:
  - `python3 -m py_compile` on changed Python files
  - `bash -n` on changed cron wrapper scripts
  - JSON validation for OpenClaw config files touched during rollout
- Targeted tests passed:
  - `pytest -q tests/unit/test_reconcile_orders.py tests/unit/test_cron_summary_outputs.py`
  - result: `5 passed`
- Runtime smoke check passed:
  - `./scripts/cron/run_paper_demo.sh --dry-run`
  - returned a single `CRON_SUMMARY_JSON=...` line as intended

## OpenClaw Rollout Note

- Local OpenClaw config was updated outside the repo to add a dedicated
  `stock_swing_cron` agent and reroute selected heavy cron jobs toward lighter-context
  execution.
- Current gateway/cron inspection showed the live scheduler still serving legacy `main`
  agent definitions for at least some existing jobs (`stock_swing_news_collection`,
  `stock_swing_order_reconciliation_market_hours`) even after restart, so the repo-side
  implementation is complete but the OpenClaw runtime rollout remains partially separate
  from this repository.
- Because `.openclaw/` is not part of the `stock_swing` Git repository, those local
  OpenClaw config changes are documented here but are not included in the GitHub push.

## Backlog Commit Cleanup

- Reviewed the remaining uncommitted `stock_swing` repo changes and separated durable
  source/docs/config updates from generated runtime artifacts.
- Added `.gitignore` entries for:
  - `artifacts/`
  - `data/backtest_price_cache/`
  - tracker backup / pending-exit runtime files under `data/tracking/`
- Ran a broad regression set covering console, tracker, rebuild, broker/source adapters,
  risk guards, and related scripts:
  - `199 passed`
- After validation, the remaining repo-side backlog changes were committed and pushed.
