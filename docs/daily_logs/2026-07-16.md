# 2026-07-16 実装ログ

## サマリー

本日は3つの改善を実施。合計 commit 3件。

---

## 改善1: broker_reconstructed 閾値自動昇格（broker_recon graduation）

### 背景
- `broker_reconstructed` トレード（entry_signal_strength=None）はデフォルト -5% stop を使用
- 5日以上保有した既存ポジションは十分に「確立済み」とみなし、-7% の標準閾値に昇格すべき
- 例: ADBE（2026-07-07 entry、9日保有）→ -5% は保守的すぎる

### 変更内容

| ファイル | 変更内容 |
|---|---|
| `src/stock_swing/strategy_engine/simple_exit_v2.py` | `_resolve_thresholds()` に `hold_days` 引数追加。hold_days >= broker_recon_graduation_days かつ signal_strength=None の場合 -5% → -7% に昇格 |
| `config/strategy/simple_exit_v2.yaml` | `broker_recon_graduation_days: 5` 追加 |
| `src/stock_swing/cli/paper_demo.py` | config から `broker_recon_graduation_days` を読み込み、`generate()` に渡すよう修正 |

### テスト
- graduation 境界 / disabled / hold_days=None の3ケース追加
- **736 passed, 2 skipped**

---

## 改善2: Broker/Tracker Reconciliation 実行（2026-07-16）

### 結果
| 項目 | 値 |
|---|---|
| Broker positions | 14件 |
| Tracker open symbols | 14件 |
| Issues | **0件**（完全一致） |
| last_success | **07-13 → 07-16 更新** |

**確認ポジション（14件）**:
ADBE, AMZN, CRM, DDOG, DELL, FTNT, META, MSFT, NOW, PANW, PATH, PLTR, SKYY, SNOW

### fix: reconciliation last_success 取得元を修正
- `broker_tracker_reconciliation.md` の Generated タイムスタンプを参照するよう変更
- インライン reconciler のスタール表示（直近注文の pending 状態が常にずれて見える問題）を修正
- **commit 4434291**

---

## 改善3: PAPER_DEMO_EXIT_ONLY フラグ追加

### 背景
- premarket（09:25 ET）は価格ノイズが高く、entry 品質が低い
- しかし保有中ポジションのストップロスは premarket でも実行すべき

### 変更内容
- `paper_demo.py`: `PAPER_DEMO_EXIT_ONLY=true` で全 buy シグナルをブロック
  - フィルターチェーンの最上流（off-hours guardrail より前）に適用
  - stop_loss / trailing_stop は通常通り実行
- premarket cron（09:25 ET）に `PAPER_DEMO_EXIT_ONLY=true` を追加

### テスト
- **736 passed, 2 skipped**（変更なし）

---

## Close Events（本日）

| Symbol | Exit Reason | PnL |
|---|---|---|
| FTNT | breakeven_stop | — |
| NOW | stop_loss | -$2,460 |
| DELL | stop_loss | -$2,436 |

**stop_loss 件数**: 2件（全て 1日以上保有後 → min_hold 正常動作確認）
- NOW: 2026-07-07 entry → 07-16 exit = **9日保有**
- DELL: 2026-07-15 entry → 07-16 exit = **1日保有**（最短、min_hold ギリギリ）

---

## Git
- `0bc0868` 改善1+2: broker_recon graduation + reconciliation 07-16
- `4434291` fix: reconciliation last_success を broker_tracker_reconciliation.md から取得
- `1f8659c` feat: PAPER_DEMO_EXIT_ONLY フラグ追加 + premarket cron に適用
